---
phase: 04-facturacion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_03_20_000005_create_billings_table.php
  - app/Enums/BillingStatus.php
  - app/Models/Billing.php
  - app/Models/Client.php
  - database/factories/BillingFactory.php
  - tests/Feature/Billing/BillingCrudTest.php
  - tests/Feature/Billing/BillingValidationTest.php
  - tests/Feature/Billing/BillingDashboardTest.php
autonomous: true
requirements:
  - BILL-01
  - BILL-02

must_haves:
  truths:
    - "Billing model exists with decimal:2 cast on monto and BillingStatus enum cast on estado"
    - "billings table has client_id with nullOnDelete, decimal(12,2) monto, nullable fecha_pago, enum estado"
    - "Client::billings() hasMany relationship exists"
    - "BillingFactory generates valid records with fecha_pago only when estado=pagado"
    - "Test stubs exist for CRUD, validation, and dashboard tests"
  artifacts:
    - path: "database/migrations/2026_03_20_000005_create_billings_table.php"
      provides: "billings table schema"
      contains: "nullOnDelete"
    - path: "app/Enums/BillingStatus.php"
      provides: "pendiente/pagado/vencido string-backed enum"
      contains: "enum BillingStatus: string"
    - path: "app/Models/Billing.php"
      provides: "Billing Eloquent model"
      contains: "decimal:2"
    - path: "app/Models/Client.php"
      provides: "billings() hasMany"
      contains: "billings"
    - path: "database/factories/BillingFactory.php"
      provides: "Test data factory"
      contains: "BillingFactory"
    - path: "tests/Feature/Billing/BillingCrudTest.php"
      provides: "CRUD test stubs"
      contains: "markTestIncomplete"
    - path: "tests/Feature/Billing/BillingValidationTest.php"
      provides: "Validation test stubs"
      contains: "markTestIncomplete"
    - path: "tests/Feature/Billing/BillingDashboardTest.php"
      provides: "Dashboard test stubs"
      contains: "markTestIncomplete"
  key_links:
    - from: "app/Models/Billing.php"
      to: "app/Enums/BillingStatus.php"
      via: "enum cast in casts()"
      pattern: "BillingStatus::class"
    - from: "app/Models/Client.php"
      to: "app/Models/Billing.php"
      via: "hasMany relationship"
      pattern: "hasMany\\(Billing::class\\)"
---

<objective>
Create the billing data foundation: migration, model, enum, factory, and test stubs.

Purpose: Establish the database schema and Eloquent model for billing records so that Plans 02 and 03 can build controllers and UI against stable contracts.
Output: Migration, Billing model with casts, BillingStatus enum, BillingFactory, Client::billings() relationship, and test stub files with markTestIncomplete.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-facturacion/04-CONTEXT.md
@.planning/phases/04-facturacion/04-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs -->

From app/Models/Client.php:
```php
class Client extends Model
{
    use HasFactory;
    // ... existing relationships: user(), invitations(), tasks()
    // ADD: billings() hasMany
}
```

From app/Enums/TaskStatus.php (pattern to mirror):
```php
enum TaskStatus: string
{
    case Backlog = 'backlog';
    case EnProgreso = 'en_progreso';
    case EnRevision = 'en_revision';
    case Finalizado = 'finalizado';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + BillingStatus enum + Billing model + Client::billings()</name>
  <files>
    database/migrations/2026_03_20_000005_create_billings_table.php,
    app/Enums/BillingStatus.php,
    app/Models/Billing.php,
    app/Models/Client.php
  </files>
  <read_first>
    app/Enums/TaskStatus.php,
    app/Models/Task.php,
    app/Models/Client.php,
    database/migrations/2026_03_20_000004_create_tasks_table.php
  </read_first>
  <action>
1. Create `app/Enums/BillingStatus.php` — string-backed enum with cases: Pendiente='pendiente', Pagado='pagado', Vencido='vencido'. Mirror TaskStatus pattern exactly.

2. Create migration `database/migrations/2026_03_20_000005_create_billings_table.php`:
   - `$table->id()`
   - `$table->foreignId('client_id')->nullable()->constrained()->nullOnDelete()` — CRITICAL: nullOnDelete, NOT cascadeOnDelete. Financial records must survive client deletion.
   - `$table->string('concepto')`
   - `$table->decimal('monto', 12, 2)`
   - `$table->date('fecha_emision')`
   - `$table->date('fecha_pago')->nullable()`
   - `$table->enum('estado', ['pendiente', 'pagado', 'vencido'])->default('pendiente')`
   - `$table->timestamps()`

3. Create `app/Models/Billing.php`:
   - `use HasFactory`
   - `$fillable`: client_id, concepto, monto, fecha_emision, fecha_pago, estado
   - `casts()` method: estado => BillingStatus::class, fecha_emision => 'date', fecha_pago => 'date', monto => 'decimal:2'
   - `client()` BelongsTo relationship

4. Add `billings()` HasMany to `app/Models/Client.php`. Add `use App\Models\Billing;` import. Place after existing `tasks()` method.

5. Run `php artisan migrate` to verify migration applies cleanly.
  </action>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan migrate --force 2>&1 && php artisan tinker --execute="echo App\Models\Billing::query()->count() . ' ok';" && php artisan tinker --execute="echo (new App\Models\Client)->billings() instanceof \Illuminate\Database\Eloquent\Relations\HasMany ? 'hasMany ok' : 'FAIL';"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "nullOnDelete" database/migrations/2026_03_20_000005_create_billings_table.php
    - grep -q "decimal.*monto.*12.*2" database/migrations/2026_03_20_000005_create_billings_table.php
    - grep -q "enum BillingStatus: string" app/Enums/BillingStatus.php
    - grep -q "'monto'.*=>.*'decimal:2'" app/Models/Billing.php
    - grep -q "BillingStatus::class" app/Models/Billing.php
    - grep -q "function billings" app/Models/Client.php
    - grep -q "hasMany(Billing::class)" app/Models/Client.php
  </acceptance_criteria>
  <done>billings table exists in DB with correct schema, Billing model has decimal:2 cast and BillingStatus enum cast, Client::billings() returns HasMany</done>
</task>

<task type="auto">
  <name>Task 2: BillingFactory + test stubs (Nyquist Wave 0)</name>
  <files>
    database/factories/BillingFactory.php,
    tests/Feature/Billing/BillingCrudTest.php,
    tests/Feature/Billing/BillingValidationTest.php,
    tests/Feature/Billing/BillingDashboardTest.php
  </files>
  <read_first>
    database/factories/TaskFactory.php,
    tests/Feature/Tasks/TaskCrudTest.php,
    tests/Feature/Clients/ClientCrudTest.php
  </read_first>
  <action>
1. Create `database/factories/BillingFactory.php`:
   - `client_id` => Client::factory()
   - `concepto` => fake()->sentence(3)
   - `monto` => fake()->randomFloat(2, 100, 50000)
   - `fecha_emision` => fake()->dateTimeBetween('-6 months', 'now')->format('Y-m-d')
   - `fecha_pago` => conditionally set: if estado is 'pagado', generate a date; otherwise null
   - `estado` defaults to 'pendiente' (NOT random — predictable for tests, same pattern as TaskFactory defaulting to backlog)
   - Add state methods: `->pagado()` sets estado=pagado + fecha_pago=today, `->vencido()` sets estado=vencido + fecha_pago=null, `->pendiente()` sets estado=pendiente + fecha_pago=null

2. Create `tests/Feature/Billing/BillingCrudTest.php` with stub methods (markTestIncomplete):
   - test_admin_can_view_billing_index
   - test_admin_can_create_billing
   - test_admin_can_update_billing
   - test_admin_can_delete_billing
   - test_guest_cannot_access_billing (redirects to login)
   Each stub: `$this->markTestIncomplete('Pending BillingController');`

3. Create `tests/Feature/Billing/BillingValidationTest.php` with stub methods:
   - test_store_requires_concepto_monto_fecha_emision_estado
   - test_fecha_pago_required_when_estado_is_pagado
   - test_fecha_pago_not_required_when_estado_is_pendiente
   - test_estado_rejects_invalid_values
   Each stub: `$this->markTestIncomplete('Pending BillingController');`

4. Create `tests/Feature/Billing/BillingDashboardTest.php` with stub methods:
   - test_summary_shows_correct_cobrado_mes
   - test_cobrado_mes_excludes_other_months
   - test_pendiente_total_sums_correctly
   - test_filter_by_estado_returns_correct_billings
   - test_filter_by_cliente_returns_correct_billings
   - test_client_show_includes_billings
   Each stub: `$this->markTestIncomplete('Pending BillingController');`

All test classes: `use RefreshDatabase`, `use App\Models\Billing`, `use App\Models\Client`, `use App\Models\User`, `use App\Enums\Role`. Create admin user helper: `User::factory()->create(['role' => Role::Admin])`.
  </action>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan test --filter=Billing 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "BillingFactory" database/factories/BillingFactory.php
    - grep -q "markTestIncomplete" tests/Feature/Billing/BillingCrudTest.php
    - grep -q "markTestIncomplete" tests/Feature/Billing/BillingValidationTest.php
    - grep -q "markTestIncomplete" tests/Feature/Billing/BillingDashboardTest.php
    - grep -q "test_fecha_pago_required_when_estado_is_pagado" tests/Feature/Billing/BillingValidationTest.php
    - grep -q "test_summary_shows_correct_cobrado_mes" tests/Feature/Billing/BillingDashboardTest.php
    - grep -q "test_client_show_includes_billings" tests/Feature/Billing/BillingDashboardTest.php
  </acceptance_criteria>
  <done>BillingFactory creates valid records, all 15 test stubs run as incomplete (0 failures), php artisan test full suite still green</done>
</task>

</tasks>

<verification>
- `php artisan migrate:fresh --seed` runs without error
- `php artisan test --filter=Billing` shows all tests incomplete (not failed)
- `php artisan test` full suite green (existing tests unbroken)
- Billing::factory()->create() produces valid row in DB
- Client::find(X)->billings returns Collection
</verification>

<success_criteria>
- billings table exists with nullOnDelete FK, decimal(12,2) monto, enum estado
- BillingStatus enum has 3 cases (Pendiente, Pagado, Vencido)
- Billing model casts: monto as decimal:2, estado as BillingStatus, dates as date
- Client::billings() hasMany works
- BillingFactory produces valid records
- 15 test stubs exist across 3 test files, all markTestIncomplete
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-facturacion/04-01-SUMMARY.md`
</output>
