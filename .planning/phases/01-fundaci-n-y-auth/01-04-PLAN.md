---
phase: 01-fundaci-n-y-auth
plan: 04
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - resources/js/Layouts/AdminLayout.vue
  - resources/js/Pages/Auth/Login.vue
  - routes/web.php
  - routes/auth.php
  - app/Providers/AppServiceProvider.php
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "Full flow works end-to-end: admin login -> dashboard -> create invitation -> copy link -> client accepts -> portal"
    - "Logout works from both AdminLayout and PortalLayout"
    - "Session persists across browser reload for both roles"
    - "Breeze default /register route is disabled (clients register only via invitation)"
    - "AdminLayout navigation includes link to Invitations"
    - "All 14 tests pass with zero failures"
  artifacts:
    - path: "routes/web.php"
      provides: "Complete route file with all groups"
      contains: "invitations.create"
    - path: "resources/js/Layouts/AdminLayout.vue"
      provides: "Admin layout with complete navigation"
      contains: "invitations/create"
  key_links:
    - from: "resources/js/Layouts/AdminLayout.vue"
      to: "/invitations/create"
      via: "Inertia Link component"
      pattern: "invitations/create"
    - from: "routes/web.php"
      to: "all controllers"
      via: "route definitions"
      pattern: "InvitationController"
---

<objective>
Final wiring: disable Breeze default registration (clients register only via invitation), add invitation nav link to AdminLayout, verify full end-to-end flow, and run complete test suite.

Purpose: This closes all remaining gaps and ensures the auth system works as a complete unit. Covers all 5 AUTH requirements in the final integration check.
Output: Phase 1 complete -- all auth requirements met, all tests green, ready for visual verification.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaci-n-y-auth/01-CONTEXT.md
@.planning/phases/01-fundaci-n-y-auth/01-RESEARCH.md
@.planning/phases/01-fundaci-n-y-auth/01-01-SUMMARY.md
@.planning/phases/01-fundaci-n-y-auth/01-02-SUMMARY.md
@.planning/phases/01-fundaci-n-y-auth/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable default registration, add invitation nav link, wire final routes, run full test suite</name>
  <files>
    routes/web.php,
    routes/auth.php,
    resources/js/Layouts/AdminLayout.vue,
    resources/js/Pages/Auth/Login.vue,
    app/Providers/AppServiceProvider.php
  </files>
  <read_first>
    routes/web.php (full current state -- understand all route groups)
    routes/auth.php (Breeze auth routes -- find /register routes to disable)
    resources/js/Layouts/AdminLayout.vue (current nav structure -- add invitation link)
    resources/js/Pages/Auth/Login.vue (check if it has a "Register" link to remove)
    app/Providers/AppServiceProvider.php (may need to disable registration feature)
  </read_first>
  <action>
    1. Disable Breeze's default `/register` route:
       - Open `routes/auth.php`
       - Comment out or remove the two register routes:
         - `Route::get('register', [RegisteredUserController::class, 'create'])`
         - `Route::post('register', [RegisteredUserController::class, 'store'])`
       - Add a comment: `// Registration disabled — clients register via invitation only`
       - Clients should NOT be able to self-register. The only path to a client account is through an admin invitation.

    2. Update `resources/js/Pages/Auth/Login.vue`:
       - Read the current file to find any "Register" or "Don't have an account?" link
       - Remove or hide any link pointing to `/register`
       - The login page should show only the login form (email, password, remember me, submit)

    3. Update `resources/js/Layouts/AdminLayout.vue` navigation:
       - Add "Invitar Cliente" as a nav link pointing to `/invitations/create`
       - Place it after "Dashboard" in the nav bar
       - Use Inertia `Link` component: `<Link href="/invitations/create">Invitar Cliente</Link>`
       - Add active state: highlight when current URL starts with `/invitations`

    4. Verify `APP_URL` is set correctly in `.env`:
       - Must be `http://hub-srojas.test` for Laragon
       - If it's `http://localhost`, signed URLs will fail (Pitfall 2 from research)
       - Read `.env` and verify. If wrong, fix it.

    5. Run the complete test suite:
       ```bash
       php artisan test
       ```
       All 14 tests (LoginTest: 3, MiddlewareTest: 5, InvitationTest: 3, ClientRegistrationTest: 3) must pass with zero failures.

    6. Run `npm run build` to verify all Vue files compile clean.

    7. Verify route list is clean:
       ```bash
       php artisan route:list --compact
       ```
       Expected routes:
       - GET /login (Breeze)
       - POST /login (Breeze)
       - POST /logout (Breeze)
       - GET /dashboard (admin middleware)
       - GET /portal (client middleware)
       - GET /invitations/create (admin middleware)
       - POST /invitations (admin middleware)
       - GET /invitation/accept (signed middleware)
       - POST /invitation/accept (signed middleware)
       - NO /register routes
  </action>
  <verify>
    <automated>php artisan test 2>&1 | tail -10 && php artisan route:list --compact 2>&1 | grep -c "register" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `routes/auth.php` does NOT contain active `register` route (commented out or removed)
    - `resources/js/Pages/Auth/Login.vue` does NOT contain a link to `/register`
    - `resources/js/Layouts/AdminLayout.vue` contains link to `/invitations/create` with text "Invitar Cliente"
    - `.env` contains `APP_URL=http://hub-srojas.test`
    - `php artisan test` shows 14 tests, 0 failures
    - `php artisan route:list` shows NO `/register` routes
    - `php artisan route:list` shows `/invitations/create` with admin middleware
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Registration disabled, admin nav complete with invitation link, APP_URL correct, all 14 tests green, Vite builds clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete auth flow</name>
  <files>N/A -- verification only, no files modified</files>
  <action>
    This is a human verification checkpoint. The executor should start the dev server (`npm run dev`) and present the verification steps below to the user. No code changes needed.
  </action>
  <what-built>
    Complete authentication system with:
    - Admin login with role-based redirect
    - Admin dashboard with AdminLayout (nav: Dashboard, Invitar Cliente, logout)
    - Invitation creation (admin generates signed link, copies it)
    - Client registration via invitation link (pre-filled name/email, sets password)
    - Client portal with PortalLayout (nav: Portal, logout)
    - Role-based middleware (admin cannot access /portal, client cannot access /dashboard)
    - Custom 403 error page (Inertia, not Blade)
    - Session persistence across reload
    - Logout from any page
  </what-built>
  <how-to-verify>
    Prerequisites: Start Vite dev server with `npm run dev` and ensure Laragon is serving the app at http://hub-srojas.test

    1. **Admin Login (AUTH-01, AUTH-04):**
       - Visit http://hub-srojas.test/login
       - Login with ADMIN_EMAIL / ADMIN_PASSWORD from .env
       - Verify redirect to /dashboard
       - Verify AdminLayout is visible (nav bar with "Dashboard", "Invitar Cliente", user name, "Cerrar sesion")
       - Reload the page -- verify session persists (still on /dashboard, still authenticated)

    2. **Create Invitation (AUTH-02):**
       - Click "Invitar Cliente" in the nav
       - Fill in: Name = "Test Client", Email = "test@client.com"
       - Click "Generar Link de Invitacion"
       - Verify a green box appears with the signed URL
       - Copy the URL

    3. **Client Registration (AUTH-03):**
       - Open a new incognito/private window
       - Paste the copied invitation URL
       - Verify the Accept page shows with pre-filled name ("Test Client") and email ("test@client.com") -- both disabled
       - Enter password (min 8 chars) and confirm
       - Click "Crear mi cuenta"
       - Verify redirect to /portal
       - Verify PortalLayout is visible (different from AdminLayout)
       - Verify "Mi Portal" heading is shown

    4. **Role Separation:**
       - As the client (in /portal), manually navigate to http://hub-srojas.test/dashboard
       - Verify 403 error page appears with "No tenes acceso a esta pagina." message
       - Go back to /portal

    5. **Logout (AUTH-05):**
       - Click "Cerrar sesion" in the portal layout
       - Verify redirect to / or /login
       - Verify you are logged out (visiting /portal redirects to /login)

    6. **Used Invitation:**
       - Paste the same invitation URL again
       - Verify 403 with "Esta invitacion ya fue utilizada" message (not a 404)

    7. **Admin Logout:**
       - Go back to the first window (admin)
       - Click "Cerrar sesion"
       - Verify logged out
  </how-to-verify>
  <verify>
    <automated>echo "Human verification -- no automated check"</automated>
  </verify>
  <done>User has approved the visual verification of the complete auth flow covering AUTH-01 through AUTH-05</done>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- All 14 PHPUnit tests pass
- No /register route exists
- Full end-to-end flow verified by human
- Admin and client see different layouts
- 403 page is Inertia (not Blade HTML)
- Signed URL validation works (expired = 403, used = 403 with message)
- Session persists across reload
- Logout works from both layouts
</verification>

<success_criteria>
Phase 1 complete: all 5 AUTH requirements verified (AUTH-01 through AUTH-05), 14 tests green, visual verification approved by user.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-y-auth/01-04-SUMMARY.md`
</output>
