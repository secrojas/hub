---
phase: 01-fundaci-n-y-auth
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - composer.json
  - package.json
  - .env
  - bootstrap/app.php
  - app/Enums/Role.php
  - app/Models/User.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - database/migrations/0001_01_01_000000_create_users_table.php
  - database/migrations/2026_03_19_000001_add_role_to_users_table.php
  - database/migrations/2026_03_19_000002_create_invitations_table.php
  - database/seeders/DatabaseSeeder.php
  - resources/js/app.js
  - tests/Feature/Auth/LoginTest.php
  - tests/Feature/Auth/InvitationTest.php
  - tests/Feature/Auth/ClientRegistrationTest.php
  - tests/Feature/Auth/MiddlewareTest.php
autonomous: true
requirements: [AUTH-01, AUTH-04]

must_haves:
  truths:
    - "Laravel 11 is installed and `php artisan serve` boots without errors"
    - "Breeze Vue+Inertia pages render (login page loads at /login)"
    - "Database `hub` has users table with role enum column"
    - "Database has invitations table with token, email, client_name, expires_at, used_at"
    - "Admin user is seeded from .env vars and can be found in DB"
    - "Test stubs exist and run (all marked pending/skip)"
  artifacts:
    - path: "app/Enums/Role.php"
      provides: "Role enum with admin/client values"
      contains: "enum Role: string"
    - path: "app/Models/User.php"
      provides: "User model with role cast"
      contains: "Role::class"
    - path: "database/seeders/DatabaseSeeder.php"
      provides: "Admin seeder from .env"
      contains: "env('ADMIN_EMAIL')"
    - path: "bootstrap/app.php"
      provides: "HandleInertiaRequests in web middleware"
      contains: "HandleInertiaRequests::class"
  key_links:
    - from: "app/Enums/Role.php"
      to: "app/Models/User.php"
      via: "Eloquent cast"
      pattern: "'role'\\s*=>\\s*Role::class"
    - from: "database/seeders/DatabaseSeeder.php"
      to: ".env"
      via: "env() helper"
      pattern: "env\\('ADMIN_EMAIL'\\)"
---

<objective>
Scaffold the entire Laravel 11 + Breeze + Vue + Inertia foundation, create the database schema (users with role enum, invitations table), seed the admin user, and create test stubs for all auth requirements.

Purpose: Every subsequent plan depends on a working Laravel app with Breeze auth pages, the role system, and the invitations table. This is the foundation.
Output: Working Laravel app with Breeze login page rendering, database migrated with role + invitations, admin seeded, test stubs ready.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaci-n-y-auth/01-CONTEXT.md
@.planning/phases/01-fundaci-n-y-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Laravel 11 + Breeze + Vue/Inertia and configure environment</name>
  <files>
    composer.json, package.json, .env, bootstrap/app.php,
    app/Http/Middleware/HandleInertiaRequests.php, resources/js/app.js
  </files>
  <read_first>
    .planning/phases/01-fundaci-n-y-auth/01-RESEARCH.md (Standard Stack section + Pattern 1 for HandleInertiaRequests verification)
    .planning/phases/01-fundaci-n-y-auth/01-CONTEXT.md (Installation decisions)
  </read_first>
  <action>
    1. Run `composer create-project laravel/laravel . --prefer-dist` in the existing repo directory `C:/laragon/www/hub-srojas`.
       - IMPORTANT: The `.planning/` directory and `.git/` already exist. The composer command will install Laravel around them. Verify `.planning/` is intact after install.

    2. Configure `.env`:
       - Set `DB_DATABASE=hub`
       - Set `DB_USERNAME=root` (Laragon default)
       - Set `DB_PASSWORD=` (Laragon default, empty)
       - Set `APP_URL=http://hub-srojas.test` (CRITICAL for signed URLs -- must match Laragon virtual host)
       - Add `ADMIN_EMAIL=admin@hub.test`
       - Add `ADMIN_PASSWORD=password`
       - Set `APP_NAME=Hub`

    3. Create the `hub` database if it does not exist:
       - Run `php artisan db:show` or use mysql CLI: `mysql -u root -e "CREATE DATABASE IF NOT EXISTS hub;"`

    4. Install Breeze with Vue + Inertia CSR:
       ```bash
       composer require laravel/breeze --dev
       php artisan breeze:install vue --no-interaction
       npm install
       ```
       Do NOT add `--ssr` or `--typescript` flags.

    5. CRITICAL VERIFICATION -- Check `bootstrap/app.php` for HandleInertiaRequests:
       - Open `bootstrap/app.php`
       - Confirm `\App\Http\Middleware\HandleInertiaRequests::class` is in `$middleware->web(append: [...])`
       - Confirm `\Illuminate\Http\Middleware\AddLinkHeadersForPreloadedAssets::class` is also present
       - If missing, add them manually inside `->withMiddleware(function (Middleware $middleware) { ... })`

    6. Verify `resources/js/app.js` uses `resolvePageComponent` from `laravel-vite-plugin/inertia-helpers`. Do not modify the Breeze-generated resolve pattern. Optionally update the title callback to: `title: (title) => title ? \`\${title} - Hub\` : 'Hub'`

    7. Run `php artisan migrate` to create default Laravel tables.

    8. Run `npm run build` to verify Vite builds without errors.

    9. Add to `.gitignore` if not already present: `node_modules/`, `vendor/`, `.env`
  </action>
  <verify>
    <automated>php artisan --version && php artisan route:list --compact 2>/dev/null | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan --version` outputs "Laravel Framework 11.x.x"
    - `bootstrap/app.php` contains the string `HandleInertiaRequests::class`
    - `.env` contains `DB_DATABASE=hub`
    - `.env` contains `APP_URL=http://hub-srojas.test`
    - `.env` contains `ADMIN_EMAIL=`
    - `.env` contains `ADMIN_PASSWORD=`
    - `npm run build` completes without errors
    - `php artisan route:list` shows `/login` and `/register` routes (from Breeze)
    - `.planning/` directory still exists with all files intact
  </acceptance_criteria>
  <done>Laravel 11 + Breeze installed, .env configured for Laragon, HandleInertiaRequests verified in middleware, Vite builds clean, login page accessible</done>
</task>

<task type="auto">
  <name>Task 2: Create Role enum, migrations (role + invitations), User model cast, admin seeder, and test stubs</name>
  <files>
    app/Enums/Role.php, app/Models/User.php,
    database/migrations/2026_03_19_000001_add_role_to_users_table.php,
    database/migrations/2026_03_19_000002_create_invitations_table.php,
    app/Models/Invitation.php,
    database/seeders/DatabaseSeeder.php,
    tests/Feature/Auth/LoginTest.php,
    tests/Feature/Auth/InvitationTest.php,
    tests/Feature/Auth/ClientRegistrationTest.php,
    tests/Feature/Auth/MiddlewareTest.php
  </files>
  <read_first>
    app/Models/User.php (current Breeze-generated User model -- preserve existing content)
    database/seeders/DatabaseSeeder.php (current seeder -- preserve structure)
    bootstrap/app.php (confirm middleware state from Task 1)
    .planning/phases/01-fundaci-n-y-auth/01-RESEARCH.md (Pattern 2: Role enum, migration examples, seeder example)
    .planning/phases/01-fundaci-n-y-auth/01-VALIDATION.md (Wave 0 requirements for test stubs)
    phpunit.xml (verify test DB config -- should use sqlite :memory: for tests)
  </read_first>
  <action>
    1. Create `app/Enums/Role.php`:
       ```php
       namespace App\Enums;

       enum Role: string
       {
           case Admin  = 'admin';
           case Client = 'client';
       }
       ```

    2. Create migration `database/migrations/2026_03_19_000001_add_role_to_users_table.php`:
       - Add `$table->enum('role', array_column(\App\Enums\Role::cases(), 'value'))->default(\App\Enums\Role::Client->value)->after('email');`
       - Down: `$table->dropColumn('role');`

    3. Create migration `database/migrations/2026_03_19_000002_create_invitations_table.php`:
       - `$table->id();`
       - `$table->string('token')->unique();`
       - `$table->string('email');`
       - `$table->string('client_name');`
       - `$table->timestamp('expires_at');`
       - `$table->timestamp('used_at')->nullable();`
       - `$table->timestamps();`

    4. Create `app/Models/Invitation.php`:
       - Fillable: `token`, `email`, `client_name`, `expires_at`, `used_at`
       - Casts: `expires_at` => `datetime`, `used_at` => `datetime`

    5. Modify `app/Models/User.php`:
       - Add `use App\Enums\Role;` import
       - Add `'role' => Role::class` to the `$casts` array (merge with existing casts, keep `email_verified_at` and `password`)
       - Add helper method: `public function isAdmin(): bool { return $this->role === Role::Admin; }`
       - Add helper method: `public function isClient(): bool { return $this->role === Role::Client; }`

    6. Update `database/seeders/DatabaseSeeder.php`:
       ```php
       use App\Enums\Role;
       use Illuminate\Support\Facades\Hash;

       public function run(): void
       {
           \App\Models\User::firstOrCreate(
               ['email' => env('ADMIN_EMAIL')],
               [
                   'name'              => 'Admin',
                   'password'          => Hash::make(env('ADMIN_PASSWORD')),
                   'role'              => Role::Admin,
                   'email_verified_at' => now(),
               ]
           );
       }
       ```

    7. Run `php artisan migrate` to apply the two new migrations.

    8. Run `php artisan db:seed` to create the admin user.

    9. Verify `phpunit.xml` -- ensure it uses SQLite in-memory for tests:
       - Check for `<env name="DB_CONNECTION" value="sqlite"/>` and `<env name="DB_DATABASE" value=":memory:"/>` in the `<php>` section
       - If not present, add them

    10. Create test stubs (these are SKELETON tests that will be filled in by subsequent plans):

        `tests/Feature/Auth/LoginTest.php`:
        - `test_admin_can_login_with_valid_credentials` -- POST /login with admin creds, assert redirect to /dashboard
        - `test_admin_cannot_login_with_wrong_password` -- POST /login with wrong pw, assert session has errors
        - `test_users_can_logout` -- POST /logout as authenticated user, assert redirect + guest

        `tests/Feature/Auth/InvitationTest.php`:
        - `test_admin_can_generate_invitation_link` -- POST invitation create as admin, assert session has invitation_url
        - `test_expired_invitation_is_rejected` -- GET signed URL with past expiry, assert 403
        - `test_used_invitation_is_rejected` -- mark invitation used_at, GET link, assert 403 message

        `tests/Feature/Auth/ClientRegistrationTest.php`:
        - `test_client_can_accept_valid_invitation` -- GET signed URL, assert 200 + see Accept page
        - `test_client_can_set_password_and_register` -- POST signed URL with password, assert user created with role=client
        - `test_client_form_has_prefilled_email_and_name` -- GET signed URL, assert props contain email and client_name

        `tests/Feature/Auth/MiddlewareTest.php`:
        - `test_admin_can_access_dashboard` -- GET /dashboard as admin, assert 200
        - `test_client_cannot_access_dashboard` -- GET /dashboard as client, assert 403
        - `test_client_can_access_portal` -- GET /portal as client, assert 200
        - `test_admin_cannot_access_portal` -- GET /portal as admin, assert 403
        - `test_guest_is_redirected_to_login` -- GET /dashboard as guest, assert redirect to /login

        Each test should be a proper PHPUnit test class extending `Tests\TestCase` and using `Illuminate\Foundation\Testing\RefreshDatabase`. Mark test bodies with `$this->markTestIncomplete('Pending implementation in Plan 02/03');` for now -- they will be completed when the corresponding features are built.

    11. Run `php artisan test` to verify all stubs are recognized (should show incomplete/skipped, zero failures).
  </action>
  <verify>
    <automated>php artisan test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Enums/Role.php` exists and contains `case Admin  = 'admin';` and `case Client = 'client';`
    - File `app/Models/User.php` contains `'role' => Role::class` in casts
    - File `app/Models/User.php` contains `public function isAdmin(): bool`
    - File `app/Models/Invitation.php` exists and contains `'token', 'email', 'client_name', 'expires_at', 'used_at'` in fillable
    - Migration for `add_role_to_users_table` exists and contains `$table->enum('role'`
    - Migration for `create_invitations_table` exists and contains `$table->string('token')->unique()`
    - `database/seeders/DatabaseSeeder.php` contains `env('ADMIN_EMAIL')` and `env('ADMIN_PASSWORD')` (no hardcoded values)
    - `database/seeders/DatabaseSeeder.php` contains `firstOrCreate` (idempotent)
    - `php artisan db:seed` runs without error
    - `php artisan tinker --execute="echo \App\Models\User::where('email', env('ADMIN_EMAIL'))->first()->role->value;"` outputs `admin`
    - `tests/Feature/Auth/LoginTest.php` exists with 3 test methods
    - `tests/Feature/Auth/InvitationTest.php` exists with 3 test methods
    - `tests/Feature/Auth/ClientRegistrationTest.php` exists with 3 test methods
    - `tests/Feature/Auth/MiddlewareTest.php` exists with 5 test methods
    - `php artisan test` runs with zero failures (incomplete/skipped OK)
    - `phpunit.xml` contains `DB_CONNECTION` set to `sqlite` and `DB_DATABASE` set to `:memory:`
  </acceptance_criteria>
  <done>Role enum created, both migrations applied, Invitation model exists, admin seeded from .env, User model has role cast + helpers, 14 test stubs recognized by PHPUnit with zero failures</done>
</task>

</tasks>

<verification>
- `php artisan --version` shows Laravel 11.x
- `php artisan route:list` includes Breeze auth routes (/login, /register, /logout)
- `npm run build` completes without errors
- `php artisan migrate:status` shows all migrations ran
- `php artisan db:seed` is idempotent (can run multiple times)
- `php artisan test` shows 14 test methods, zero failures
- `bootstrap/app.php` contains HandleInertiaRequests in web middleware
</verification>

<success_criteria>
Working Laravel 11 + Breeze application with login page rendering, database migrated with role enum on users + invitations table, admin user seeded from .env, and 14 test stubs ready for subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-y-auth/01-01-SUMMARY.md`
</output>
