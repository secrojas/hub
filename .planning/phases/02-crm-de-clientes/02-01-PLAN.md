---
phase: 02-crm-de-clientes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_03_20_000001_create_clients_table.php
  - database/migrations/2026_03_20_000002_add_client_id_to_users_table.php
  - database/migrations/2026_03_20_000003_add_client_id_to_invitations_table.php
  - app/Models/Client.php
  - app/Models/User.php
  - app/Models/Invitation.php
  - database/factories/ClientFactory.php
  - tests/Feature/Clients/ClientTest.php
  - tests/Feature/Clients/ClientCrudTest.php
  - tests/Feature/Clients/ClientInvitationTest.php
autonomous: true
requirements:
  - CLIE-01
  - CLIE-02

must_haves:
  truths:
    - "La tabla clients existe con todos los campos: nombre, email (unique), empresa, telefono, stack_tecnologico, estado (enum activo/potencial/pausado default activo), notas, fecha_inicio"
    - "La tabla users tiene columna client_id nullable con FK a clients.id y nullOnDelete"
    - "La tabla invitations tiene columna client_id nullable con FK a clients.id y nullOnDelete"
    - "User model acepta client_id en mass assignment y tiene relacion client() belongsTo"
    - "Invitation model acepta client_id en mass assignment y tiene relacion client() belongsTo"
    - "Client model tiene relaciones user() hasOne e invitations() hasMany"
  artifacts:
    - path: "database/migrations/2026_03_20_000001_create_clients_table.php"
      provides: "Tabla clients con todos los campos"
      contains: "Schema::create('clients'"
    - path: "app/Models/Client.php"
      provides: "Eloquent model con fillable, casts, relationships"
      exports: ["Client"]
    - path: "database/factories/ClientFactory.php"
      provides: "Factory para tests"
      contains: "ClientFactory"
    - path: "tests/Feature/Clients/ClientTest.php"
      provides: "Test stubs para CLIE-01, CLIE-02"
  key_links:
    - from: "app/Models/User.php"
      to: "app/Models/Client.php"
      via: "belongsTo relationship + client_id in fillable"
      pattern: "belongsTo\\(Client::class\\)"
    - from: "app/Models/Invitation.php"
      to: "app/Models/Client.php"
      via: "belongsTo relationship + client_id in fillable"
      pattern: "belongsTo\\(Client::class\\)"
---

<objective>
Create the database foundation for the CRM: clients table, FK columns on users and invitations, Client model with relationships, and update existing User/Invitation models. Also create test stubs and factory for all subsequent plans.

Purpose: All CRUD plans and invitation wiring depend on these migrations and model relationships existing first.
Output: 3 migrations, 1 new model, 2 updated models, 1 factory, 3 test stub files
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crm-de-clientes/02-CONTEXT.md
@.planning/phases/02-crm-de-clientes/02-RESEARCH.md

<interfaces>
<!-- Existing models that will be modified -->

From app/Models/User.php:
```php
protected $fillable = [
    'name',
    'email',
    'password',
    'role',
];
// Methods: isAdmin(), isClient()
// Casts: email_verified_at, password, role => Role::class
```

From app/Models/Invitation.php:
```php
protected $fillable = [
    'token',
    'email',
    'client_name',
    'expires_at',
    'used_at',
];
// Casts: expires_at => datetime, used_at => datetime
```

From app/Enums/Role.php:
```php
enum Role: string {
    case Admin = 'admin';
    case Client = 'client';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migrations, Client model, and ClientFactory</name>
  <read_first>
    - database/migrations/2026_03_19_000002_create_invitations_table.php (understand existing migration pattern)
    - app/Models/User.php (current fillable, will be modified in Task 2)
    - app/Models/Invitation.php (current fillable, will be modified in Task 2)
  </read_first>
  <files>
    database/migrations/2026_03_20_000001_create_clients_table.php
    database/migrations/2026_03_20_000002_add_client_id_to_users_table.php
    database/migrations/2026_03_20_000003_add_client_id_to_invitations_table.php
    app/Models/Client.php
    database/factories/ClientFactory.php
  </files>
  <action>
    1. Create migration `2026_03_20_000001_create_clients_table.php`:
       - `$table->id()`
       - `$table->string('nombre')`
       - `$table->string('email')->unique()`
       - `$table->string('empresa')->nullable()`
       - `$table->string('telefono')->nullable()`
       - `$table->text('stack_tecnologico')->nullable()`
       - `$table->enum('estado', ['activo', 'potencial', 'pausado'])->default('activo')`
       - `$table->text('notas')->nullable()`
       - `$table->date('fecha_inicio')->nullable()`
       - `$table->timestamps()`

    2. Create migration `2026_03_20_000002_add_client_id_to_users_table.php`:
       - `$table->foreignId('client_id')->nullable()->after('role')->constrained()->nullOnDelete()`
       - Down: `$table->dropConstrainedForeignId('client_id')`

    3. Create migration `2026_03_20_000003_add_client_id_to_invitations_table.php`:
       - `$table->foreignId('client_id')->nullable()->after('client_name')->constrained()->nullOnDelete()`
       - Down: `$table->dropConstrainedForeignId('client_id')`

    4. Create `app/Models/Client.php`:
       ```php
       namespace App\Models;
       use Illuminate\Database\Eloquent\Factories\HasFactory;
       use Illuminate\Database\Eloquent\Model;
       use Illuminate\Database\Eloquent\Relations\HasOne;
       use Illuminate\Database\Eloquent\Relations\HasMany;

       class Client extends Model
       {
           use HasFactory;

           protected $fillable = [
               'nombre', 'email', 'empresa', 'telefono',
               'stack_tecnologico', 'estado', 'notas', 'fecha_inicio',
           ];

           protected function casts(): array
           {
               return [
                   'fecha_inicio' => 'date',
               ];
           }

           public function user(): HasOne
           {
               return $this->hasOne(User::class);
           }

           public function invitations(): HasMany
           {
               return $this->hasMany(Invitation::class);
           }
       }
       ```

    5. Create `database/factories/ClientFactory.php`:
       - `nombre` => `fake()->name()`
       - `email` => `fake()->unique()->safeEmail()`
       - `empresa` => `fake()->company()`
       - `telefono` => `fake()->phoneNumber()`
       - `stack_tecnologico` => `fake()->sentence()`
       - `estado` => `fake()->randomElement(['activo', 'potencial', 'pausado'])`
       - `notas` => `fake()->paragraph()`
       - `fecha_inicio` => `fake()->date()`
  </action>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan migrate --force && php artisan tinker --execute="echo App\Models\Client::query()->toRawSql();"</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate` runs without errors
    - `php artisan tinker --execute="use Illuminate\Support\Facades\Schema; echo json_encode(Schema::getColumnListing('clients'));"` outputs all 10 columns (id, nombre, email, empresa, telefono, stack_tecnologico, estado, notas, fecha_inicio, created_at, updated_at)
    - `php artisan tinker --execute="use Illuminate\Support\Facades\Schema; echo in_array('client_id', Schema::getColumnListing('users')) ? 'yes' : 'no';"` outputs "yes"
    - `php artisan tinker --execute="use Illuminate\Support\Facades\Schema; echo in_array('client_id', Schema::getColumnListing('invitations')) ? 'yes' : 'no';"` outputs "yes"
    - `app/Models/Client.php` exists with `HasFactory` trait, all 8 fillable fields, `fecha_inicio` date cast, `user()` HasOne, `invitations()` HasMany
    - `database/factories/ClientFactory.php` exists
  </acceptance_criteria>
  <done>Three migrations run successfully. Client model has all fields, casts, and relationships. ClientFactory generates valid test data.</done>
</task>

<task type="auto">
  <name>Task 2: Update User and Invitation models + create test stubs</name>
  <read_first>
    - app/Models/User.php (add client_id to fillable, add client() relationship)
    - app/Models/Invitation.php (add client_id to fillable, add client() relationship)
    - tests/Feature/Auth/InvitationTest.php (understand existing test patterns)
  </read_first>
  <files>
    app/Models/User.php
    app/Models/Invitation.php
    tests/Feature/Clients/ClientTest.php
    tests/Feature/Clients/ClientCrudTest.php
    tests/Feature/Clients/ClientInvitationTest.php
  </files>
  <action>
    1. Edit `app/Models/User.php`:
       - Add `'client_id'` to `$fillable` array (after 'role')
       - Add import: `use Illuminate\Database\Eloquent\Relations\BelongsTo;`
       - Add relationship method:
         ```php
         public function client(): BelongsTo
         {
             return $this->belongsTo(Client::class);
         }
         ```

    2. Edit `app/Models/Invitation.php`:
       - Add `'client_id'` to `$fillable` array (after 'client_name')
       - Add import: `use Illuminate\Database\Eloquent\Relations\BelongsTo;`
       - Add relationship method:
         ```php
         public function client(): BelongsTo
         {
             return $this->belongsTo(Client::class);
         }
         ```

    3. Create `tests/Feature/Clients/ClientTest.php` with test stubs:
       - `test_admin_can_create_client` — marks incomplete
       - `test_admin_can_update_client` — marks incomplete
       - `test_admin_can_delete_client` — marks incomplete
       - `test_client_stores_all_fields` — marks incomplete
       - `test_create_client_requires_nombre_and_email` — marks incomplete
       - `test_create_client_rejects_duplicate_email` — marks incomplete
       Use `RefreshDatabase` trait, create admin user in setUp with `User::factory()->create(['role' => Role::Admin])`.

    4. Create `tests/Feature/Clients/ClientCrudTest.php` with test stubs:
       - `test_admin_can_view_clients_list` — marks incomplete
       - `test_estado_filter_returns_correct_clients` — marks incomplete
       - `test_admin_can_view_client_detail` — marks incomplete
       - `test_show_page_indicates_if_client_has_active_user` — marks incomplete
       Use same pattern as ClientTest.

    5. Create `tests/Feature/Clients/ClientInvitationTest.php` with test stubs:
       - `test_invite_button_wires_invitation_with_client_id` — marks incomplete
       - `test_client_with_active_user_blocks_invite` — marks incomplete
       - `test_accept_sets_user_client_id` — marks incomplete
       Use same pattern.

    All test stubs use `$this->markTestIncomplete('Pending implementation in Plan 02/03')`.
  </action>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan test --filter=Client 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'client_id'" app/Models/User.php` outputs at least 1
    - `grep "belongsTo(Client::class)" app/Models/User.php` matches
    - `grep -c "'client_id'" app/Models/Invitation.php` outputs at least 1
    - `grep "belongsTo(Client::class)" app/Models/Invitation.php` matches
    - `php artisan test --filter=ClientTest` runs with 6 incomplete tests (0 failures)
    - `php artisan test --filter=ClientCrudTest` runs with 4 incomplete tests (0 failures)
    - `php artisan test --filter=ClientInvitationTest` runs with 3 incomplete tests (0 failures)
    - `php artisan test` full suite passes (no regressions from model changes)
  </acceptance_criteria>
  <done>User and Invitation models updated with client_id in fillable and client() belongsTo. All 13 test stubs created and running as incomplete. Full test suite green (no regressions).</done>
</task>

</tasks>

<verification>
- `php artisan migrate:status` shows all 3 new migrations as "Ran"
- `php artisan test` full suite green (existing tests + 13 incomplete stubs)
- Client model has HasFactory, all fillable fields, casts, user() and invitations() relationships
- User model has client_id in fillable and client() relationship
- Invitation model has client_id in fillable and client() relationship
</verification>

<success_criteria>
- Database schema complete: clients table, client_id FK on users, client_id FK on invitations
- All three models have correct fillable arrays and relationships
- ClientFactory generates valid data
- 13 test stubs exist and run as incomplete (0 failures)
- Full existing test suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/02-crm-de-clientes/02-01-SUMMARY.md`
</output>
