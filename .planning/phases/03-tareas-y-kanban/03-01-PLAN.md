---
phase: 03-tareas-y-kanban
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/TaskStatus.php
  - app/Enums/TaskPriority.php
  - database/migrations/2026_03_20_000004_create_tasks_table.php
  - app/Models/Task.php
  - app/Models/Client.php
  - database/factories/TaskFactory.php
  - tests/Feature/Tasks/TaskCrudTest.php
  - tests/Feature/Tasks/TaskKanbanTest.php
  - tests/Feature/Tasks/TaskFilterTest.php
autonomous: true
requirements: [TASK-01]

must_haves:
  truths:
    - "Task model exists with all required fields (titulo, client_id, descripcion, prioridad, estado, fecha_limite)"
    - "TaskStatus and TaskPriority enums exist and match DB enum values exactly"
    - "Client model has tasks() hasMany relationship"
    - "TaskFactory can generate valid tasks for all test files"
    - "Test stubs exist for CRUD, Kanban, and filter tests"
  artifacts:
    - path: "app/Enums/TaskStatus.php"
      provides: "TaskStatus backed enum (backlog, en_progreso, en_revision, finalizado)"
      contains: "enum TaskStatus: string"
    - path: "app/Enums/TaskPriority.php"
      provides: "TaskPriority backed enum (baja, media, alta)"
      contains: "enum TaskPriority: string"
    - path: "app/Models/Task.php"
      provides: "Task Eloquent model with casts and client() relationship"
      contains: "class Task extends Model"
    - path: "database/factories/TaskFactory.php"
      provides: "Factory for generating test tasks"
      contains: "class TaskFactory extends Factory"
    - path: "tests/Feature/Tasks/TaskCrudTest.php"
      provides: "Test stubs for TASK-01, TASK-02"
      contains: "class TaskCrudTest"
    - path: "tests/Feature/Tasks/TaskKanbanTest.php"
      provides: "Test stubs for TASK-03, TASK-04"
      contains: "class TaskKanbanTest"
    - path: "tests/Feature/Tasks/TaskFilterTest.php"
      provides: "Test stubs for TASK-05"
      contains: "class TaskFilterTest"
  key_links:
    - from: "app/Models/Task.php"
      to: "app/Models/Client.php"
      via: "client() belongsTo relationship"
      pattern: "belongsTo\\(Client::class\\)"
    - from: "app/Models/Client.php"
      to: "app/Models/Task.php"
      via: "tasks() hasMany relationship"
      pattern: "hasMany\\(Task::class\\)"
    - from: "app/Models/Task.php"
      to: "app/Enums/TaskStatus.php"
      via: "Eloquent cast"
      pattern: "TaskStatus::class"
---

<objective>
Create the data foundation for the tasks module: migration, model, enums, factory, and test stubs.

Purpose: All subsequent plans (controller, Kanban UI, filters) depend on the Task model, enums, and factory existing. Test stubs ensure Nyquist compliance from Wave 0.
Output: Runnable migration, Task model with relationships and casts, TaskFactory, three test stub files.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tareas-y-kanban/03-RESEARCH.md

@app/Models/Client.php
@app/Enums/Role.php
@database/factories/ClientFactory.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskStatus enum, TaskPriority enum, tasks migration, Task model, and update Client model</name>
  <files>
    app/Enums/TaskStatus.php,
    app/Enums/TaskPriority.php,
    database/migrations/2026_03_20_000004_create_tasks_table.php,
    app/Models/Task.php,
    app/Models/Client.php
  </files>
  <read_first>
    app/Enums/Role.php,
    app/Models/Client.php,
    database/migrations/*_create_clients_table.php
  </read_first>
  <action>
    1. Create `app/Enums/TaskStatus.php` — string-backed PHP enum identical to Role.php pattern:
       - `Backlog = 'backlog'`
       - `EnProgreso = 'en_progreso'`
       - `EnRevision = 'en_revision'`
       - `Finalizado = 'finalizado'`

    2. Create `app/Enums/TaskPriority.php` — string-backed PHP enum:
       - `Baja = 'baja'`
       - `Media = 'media'`
       - `Alta = 'alta'`

    3. Create migration `database/migrations/2026_03_20_000004_create_tasks_table.php`:
       ```php
       Schema::create('tasks', function (Blueprint $table) {
           $table->id();
           $table->foreignId('client_id')->constrained()->cascadeOnDelete();
           $table->string('titulo');
           $table->text('descripcion')->nullable();
           $table->enum('prioridad', ['baja', 'media', 'alta'])->default('media');
           $table->enum('estado', ['backlog', 'en_progreso', 'en_revision', 'finalizado'])->default('backlog');
           $table->date('fecha_limite')->nullable();
           $table->timestamps();
       });
       ```
       Use `cascadeOnDelete()` on client_id per research recommendation (orphaned tasks have no business value).

    4. Create `app/Models/Task.php`:
       - `use HasFactory;`
       - `$fillable`: `titulo`, `client_id`, `descripcion`, `prioridad`, `estado`, `fecha_limite`
       - `casts()` method returning: `'estado' => TaskStatus::class`, `'prioridad' => TaskPriority::class`, `'fecha_limite' => 'date'`
       - `client()` BelongsTo relationship to `Client::class`

    5. Update `app/Models/Client.php` — add `tasks()` HasMany relationship:
       ```php
       public function tasks(): HasMany
       {
           return $this->hasMany(Task::class);
       }
       ```
       Add `use App\Models\Task;` import if not present. The `HasMany` import already exists.

    6. Run `php artisan migrate` to confirm migration succeeds.
  </action>
  <acceptance_criteria>
    - grep: `enum TaskStatus: string` in app/Enums/TaskStatus.php
    - grep: `case Backlog` in app/Enums/TaskStatus.php
    - grep: `case EnProgreso` in app/Enums/TaskStatus.php
    - grep: `enum TaskPriority: string` in app/Enums/TaskPriority.php
    - grep: `case Baja` in app/Enums/TaskPriority.php
    - grep: `cascadeOnDelete` in database/migrations/2026_03_20_000004_create_tasks_table.php
    - grep: `enum.*prioridad.*baja.*media.*alta` in the migration file
    - grep: `enum.*estado.*backlog.*en_progreso.*en_revision.*finalizado` in the migration file
    - grep: `class Task extends Model` in app/Models/Task.php
    - grep: `TaskStatus::class` in app/Models/Task.php
    - grep: `TaskPriority::class` in app/Models/Task.php
    - grep: `belongsTo(Client::class)` in app/Models/Task.php
    - grep: `hasMany(Task::class)` in app/Models/Client.php
  </acceptance_criteria>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan migrate --force && php artisan tinker --execute="echo App\Models\Task::query()->toRawSql();" && php artisan test --filter=ClientTest 2>/dev/null; echo "Migration OK"</automated>
  </verify>
  <done>Migration creates tasks table with all columns. Task model has fillable, casts, client() relationship. Client model has tasks() relationship. Both enums exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskFactory and test stubs for CRUD, Kanban, and Filter tests</name>
  <files>
    database/factories/TaskFactory.php,
    tests/Feature/Tasks/TaskCrudTest.php,
    tests/Feature/Tasks/TaskKanbanTest.php,
    tests/Feature/Tasks/TaskFilterTest.php
  </files>
  <read_first>
    database/factories/ClientFactory.php,
    database/factories/UserFactory.php,
    tests/Feature/Clients/ClientCrudTest.php
  </read_first>
  <action>
    1. Create `database/factories/TaskFactory.php`:
       - `'titulo'` => `fake()->sentence(3)`
       - `'client_id'` => `Client::factory()`
       - `'descripcion'` => `fake()->optional()->paragraph()`
       - `'prioridad'` => `fake()->randomElement(['baja', 'media', 'alta'])`
       - `'estado'` => `'backlog'` (default, as per locked decision)
       - `'fecha_limite'` => `fake()->optional()->dateTimeBetween('now', '+30 days')?->format('Y-m-d')`

    2. Create `tests/Feature/Tasks/TaskCrudTest.php` with test stubs (all marked `$this->markTestIncomplete('Pending TaskController')` for now):
       - `test_admin_can_create_task` — POST /tasks with titulo + client_id, assert redirect + task exists in DB
       - `test_create_task_validation_requires_titulo_and_client` — POST /tasks with empty data, assert validation errors for titulo and client_id
       - `test_admin_can_update_task` — PUT /tasks/{id} with changed titulo, assert DB updated
       - `test_admin_can_delete_task` — DELETE /tasks/{id}, assert task gone from DB
       Each stub must: create admin user via `User::factory()->create(['role' => 'admin'])`, `actingAs($admin)`.

    3. Create `tests/Feature/Tasks/TaskKanbanTest.php` with stubs:
       - `test_index_returns_columns_scoped_to_client` — GET /tasks?cliente={id}, assert Inertia component + columns prop with only that client's tasks
       - `test_update_status_changes_task_estado` — PUT /tasks/{id}/status with estado=en_progreso, assert DB estado changed
       - `test_update_status_rejects_invalid_estado` — PUT /tasks/{id}/status with estado=invalid, assert 422
       - `test_global_view_returns_all_clients_tasks` — GET /tasks (no filter), assert all tasks present across columns

    4. Create `tests/Feature/Tasks/TaskFilterTest.php` with stubs:
       - `test_filter_by_cliente` — GET /tasks?cliente={id}, assert only that client's tasks in columns
       - `test_filter_by_estado` — GET /tasks?estado=en_progreso, assert only matching tasks
       - `test_filter_by_titulo` — GET /tasks?titulo=searchterm, assert matching tasks
       - `test_filter_by_prioridad` — GET /tasks?prioridad=alta, assert only alta tasks

    All stubs use `markTestIncomplete()` — they will be implemented in Plans 02 and 03 when the controller and routes exist.
  </action>
  <acceptance_criteria>
    - grep: `class TaskFactory extends Factory` in database/factories/TaskFactory.php
    - grep: `client_id.*Client::factory` in database/factories/TaskFactory.php
    - grep: `class TaskCrudTest` in tests/Feature/Tasks/TaskCrudTest.php
    - grep: `test_admin_can_create_task` in tests/Feature/Tasks/TaskCrudTest.php
    - grep: `test_admin_can_delete_task` in tests/Feature/Tasks/TaskCrudTest.php
    - grep: `class TaskKanbanTest` in tests/Feature/Tasks/TaskKanbanTest.php
    - grep: `test_update_status_changes_task_estado` in tests/Feature/Tasks/TaskKanbanTest.php
    - grep: `test_update_status_rejects_invalid_estado` in tests/Feature/Tasks/TaskKanbanTest.php
    - grep: `class TaskFilterTest` in tests/Feature/Tasks/TaskFilterTest.php
    - grep: `test_filter_by_titulo` in tests/Feature/Tasks/TaskFilterTest.php
    - grep: `test_filter_by_prioridad` in tests/Feature/Tasks/TaskFilterTest.php
  </acceptance_criteria>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan test --filter=Task 2>&1 | tail -5</automated>
  </verify>
  <done>TaskFactory generates valid tasks. Three test files exist with all stubs marked incomplete. `php artisan test --filter=Task` runs without errors (all incomplete, none failing).</done>
</task>

</tasks>

<verification>
- `php artisan migrate` succeeds (tasks table created)
- `php artisan tinker --execute="App\Models\Task::factory()->create();"` creates a task with a client
- `php artisan tinker --execute="App\Models\Client::first()->tasks;"` returns a Collection
- `php artisan test --filter=Task` — all stubs run as incomplete (no failures)
- `php artisan test` — full suite green (existing tests unbroken)
</verification>

<success_criteria>
- tasks table exists in DB with all 7 columns (id, client_id, titulo, descripcion, prioridad, estado, fecha_limite) + timestamps
- Task model has casts for estado (TaskStatus), prioridad (TaskPriority), fecha_limite (date)
- Client::tasks() returns HasMany relationship
- TaskFactory produces valid tasks
- 12 test stubs across 3 files, all incomplete
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-tareas-y-kanban/03-01-SUMMARY.md`
</output>
