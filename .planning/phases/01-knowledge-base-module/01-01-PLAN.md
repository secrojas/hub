---
phase: 01-knowledge-base-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_04_13_000001_create_knowledge_entries_table.php
  - database/migrations/2026_04_13_000002_create_knowledge_links_table.php
  - app/Enums/KnowledgeType.php
  - app/Enums/KnowledgeStatus.php
  - app/Enums/KnowledgeConfidence.php
  - app/Enums/KnowledgeSource.php
  - app/Enums/EmbeddingPriority.php
  - app/Enums/KnowledgeLinkRelation.php
  - app/Models/KnowledgeEntry.php
  - app/Models/KnowledgeLink.php
autonomous: true
requirements: [REQ-KB-1, REQ-KB-2]

must_haves:
  truths:
    - "knowledge_entries table exists with all schema fields (entry_id, titulo, type, status, confidence, source, verified, domain, subdomain, tags, scope, summary, contenido, avature_version, embedding_priority)"
    - "knowledge_links table exists with from_entry_id and to_entry_id FK to knowledge_entries (NOT notes)"
    - "KnowledgeEntry model casts all enum fields to PHP backed enums"
    - "KnowledgeEntry has links() and backlinks() HasMany relationships"
    - "FULLTEXT index exists on summary column"
  artifacts:
    - path: "database/migrations/2026_04_13_000001_create_knowledge_entries_table.php"
      provides: "knowledge_entries table schema"
      contains: "knowledge_entries"
    - path: "database/migrations/2026_04_13_000002_create_knowledge_links_table.php"
      provides: "knowledge_links table with FK to knowledge_entries"
      contains: "constrained('knowledge_entries')"
    - path: "app/Models/KnowledgeEntry.php"
      provides: "Eloquent model with casts and relationships"
      exports: ["KnowledgeEntry"]
    - path: "app/Models/KnowledgeLink.php"
      provides: "Eloquent model for link relationships"
      exports: ["KnowledgeLink"]
    - path: "app/Enums/KnowledgeType.php"
      provides: "concept|flow|bug|decision|runbook|glossary enum"
      exports: ["KnowledgeType"]
  key_links:
    - from: "app/Models/KnowledgeEntry.php"
      to: "app/Enums/KnowledgeType.php"
      via: "casts array"
      pattern: "'type'\\s*=>\\s*KnowledgeType::class"
    - from: "database/migrations/2026_04_13_000002_create_knowledge_links_table.php"
      to: "knowledge_entries table"
      via: "foreignId constrained"
      pattern: "constrained\\('knowledge_entries'\\)"
---

<objective>
Create the database foundation and Eloquent model layer for the Knowledge Base module.

Purpose: Establish the data layer (migrations, enums, models) that all subsequent layers (repository, service, controller, UI) depend on.
Output: Two migrations, six PHP enums, two Eloquent models — all runnable via `php artisan migrate`.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-knowledge-base-module/01-RESEARCH.md

<interfaces>
<!-- Reference patterns from Notes module — replicate exactly -->

From app/Enums/TaskStatus.php:
```php
namespace App\Enums;

enum TaskStatus: string
{
    case Backlog     = 'backlog';
    case EnProgreso  = 'en_progreso';
    case EnRevision  = 'en_revision';
    case Finalizado  = 'finalizado';
}
```

From app/Models/Note.php:
```php
protected $fillable = [
    'folder_id', 'titulo', 'contenido', 'extracto', 'esta_fijada', 'en_dashboard',
];

protected function casts(): array
{
    return [
        'esta_fijada'  => 'boolean',
        'en_dashboard' => 'boolean',
    ];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enums, migrations, and models</name>
  <files>
    app/Enums/KnowledgeType.php,
    app/Enums/KnowledgeStatus.php,
    app/Enums/KnowledgeConfidence.php,
    app/Enums/KnowledgeSource.php,
    app/Enums/EmbeddingPriority.php,
    app/Enums/KnowledgeLinkRelation.php,
    database/migrations/2026_04_13_000001_create_knowledge_entries_table.php,
    database/migrations/2026_04_13_000002_create_knowledge_links_table.php,
    app/Models/KnowledgeEntry.php,
    app/Models/KnowledgeLink.php
  </files>
  <read_first>
    app/Enums/TaskStatus.php (enum pattern reference),
    app/Models/Note.php (model pattern reference),
    docs/knowledge-system-design.md (full schema spec)
  </read_first>
  <action>
    **Step 1 — Create 6 PHP backed string enums in `app/Enums/`:**

    1. `KnowledgeType.php` — cases: `Concept = 'concept'`, `Flow = 'flow'`, `Bug = 'bug'`, `Decision = 'decision'`, `Runbook = 'runbook'`, `Glossary = 'glossary'`
    2. `KnowledgeStatus.php` — cases: `Draft = 'draft'`, `Reviewed = 'reviewed'`, `Verified = 'verified'`, `Stale = 'stale'`
    3. `KnowledgeConfidence.php` — cases: `Low = 'low'`, `Medium = 'medium'`, `High = 'high'`
    4. `KnowledgeSource.php` — cases: `Chatgpt = 'chatgpt'`, `Self = 'self'`, `Docs = 'docs'`, `Colleague = 'colleague'`
    5. `EmbeddingPriority.php` — cases: `High = 'high'`, `Normal = 'normal'`
    6. `KnowledgeLinkRelation.php` — cases: `Explains = 'explains'`, `DependsOn = 'depends_on'`, `Solves = 'solves'`, `Contradicts = 'contradicts'`, `Updates = 'updates'`, `SourceOf = 'source_of'`, `ExampleOf = 'example_of'`

    All enums follow exact pattern from `TaskStatus.php`: `namespace App\Enums;`, backed by `string`.

    **Step 2 — Create migration `2026_04_13_000001_create_knowledge_entries_table.php`:**

    ```php
    Schema::create('knowledge_entries', function (Blueprint $table) {
        $table->id();
        $table->string('entry_id')->unique();          // slug: "avt-rtc-001"
        $table->string('titulo');
        $table->string('type');                         // KnowledgeType enum
        $table->string('status')->default('draft');     // KnowledgeStatus enum
        $table->string('confidence')->default('medium');// KnowledgeConfidence enum
        $table->string('source')->default('self');      // KnowledgeSource enum
        $table->boolean('verified')->default(false);
        $table->string('domain')->nullable();
        $table->string('subdomain')->nullable();
        $table->json('tags')->nullable();
        $table->string('scope')->nullable();            // module | system | cross-system
        $table->text('summary')->nullable();
        $table->longText('contenido')->nullable();
        $table->string('avature_version')->nullable();
        $table->string('embedding_priority')->default('normal');
        $table->timestamps();

        $table->fullText('summary');                    // FULLTEXT index for BM25 search
    });
    ```

    **Step 3 — Create migration `2026_04_13_000002_create_knowledge_links_table.php`:**

    CRITICAL: FK must reference `knowledge_entries(id)`, NOT `notes(id)`.

    ```php
    Schema::create('knowledge_links', function (Blueprint $table) {
        $table->id();
        $table->foreignId('from_entry_id')->constrained('knowledge_entries')->cascadeOnDelete();
        $table->foreignId('to_entry_id')->constrained('knowledge_entries')->cascadeOnDelete();
        $table->string('relation_type');                // KnowledgeLinkRelation enum
        $table->text('notes')->nullable();
        $table->timestamps();
    });
    ```

    **Step 4 — Create `app/Models/KnowledgeEntry.php`:**

    - `namespace App\Models;`
    - `use HasFactory;`
    - `$fillable` array with ALL fields: `entry_id`, `titulo`, `type`, `status`, `confidence`, `source`, `verified`, `domain`, `subdomain`, `tags`, `scope`, `summary`, `contenido`, `avature_version`, `embedding_priority`
    - `casts()` method returning:
      - `'type' => KnowledgeType::class`
      - `'status' => KnowledgeStatus::class`
      - `'confidence' => KnowledgeConfidence::class`
      - `'source' => KnowledgeSource::class`
      - `'embedding_priority' => EmbeddingPriority::class`
      - `'verified' => 'boolean'`
      - `'tags' => 'array'`
    - `links(): HasMany` — `$this->hasMany(KnowledgeLink::class, 'from_entry_id')`
    - `backlinks(): HasMany` — `$this->hasMany(KnowledgeLink::class, 'to_entry_id')`

    **Step 5 — Create `app/Models/KnowledgeLink.php`:**

    - `namespace App\Models;`
    - `use HasFactory;`
    - `$fillable`: `from_entry_id`, `to_entry_id`, `relation_type`, `notes`
    - `casts()`: `'relation_type' => KnowledgeLinkRelation::class`
    - `fromEntry(): BelongsTo` — `$this->belongsTo(KnowledgeEntry::class, 'from_entry_id')`
    - `toEntry(): BelongsTo` — `$this->belongsTo(KnowledgeEntry::class, 'to_entry_id')`

    **Step 6 — Run migrations:**

    `php artisan migrate`
  </action>
  <verify>
    <automated>php artisan migrate --force 2>&1 && php artisan tinker --execute="echo App\Models\KnowledgeEntry::query()->toRawSql();" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge_entries` table exists with columns: id, entry_id, titulo, type, status, confidence, source, verified, domain, subdomain, tags, scope, summary, contenido, avature_version, embedding_priority, created_at, updated_at
    - `knowledge_links` table exists with columns: id, from_entry_id, to_entry_id, relation_type, notes, created_at, updated_at
    - `knowledge_links.from_entry_id` FK references `knowledge_entries.id` (NOT `notes.id`)
    - `knowledge_links.to_entry_id` FK references `knowledge_entries.id` (NOT `notes.id`)
    - FULLTEXT index exists on `knowledge_entries.summary`
    - `entry_id` column has UNIQUE constraint
    - All 6 enum files exist under `app/Enums/` with correct backed string values
    - `KnowledgeEntry` model `$fillable` includes `entry_id`
    - `KnowledgeEntry` model `casts()` maps `type => KnowledgeType::class`
    - `KnowledgeEntry` model has `links()` and `backlinks()` HasMany relationships
    - `KnowledgeLink` model has `fromEntry()` and `toEntry()` BelongsTo relationships
    - `php artisan migrate` runs without errors
  </acceptance_criteria>
  <done>Both tables created in MySQL with correct schema, all enums defined, both models functional with casts and relationships. Migration is green.</done>
</task>

</tasks>

<verification>
php artisan migrate:status | grep knowledge
php artisan tinker --execute="use App\Models\KnowledgeEntry; \$e = new KnowledgeEntry; echo implode(',', \$e->getFillable());"
</verification>

<success_criteria>
- `php artisan migrate` succeeds with both knowledge tables
- KnowledgeEntry model can be instantiated with all fillable fields
- KnowledgeLink FK points to knowledge_entries, not notes
- All 6 enum classes are loadable without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-knowledge-base-module/01-01-SUMMARY.md`
</output>
