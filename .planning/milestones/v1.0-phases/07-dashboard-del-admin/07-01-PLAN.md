---
phase: 07-dashboard-del-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/DashboardController.php
  - routes/web.php
  - resources/js/Layouts/AdminLayout.vue
  - tests/Feature/Dashboard/DashboardTest.php
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "GET /dashboard returns Inertia page with enProgreso and vencenProonto props"
    - "en_progreso tasks that also qualify as vencen pronto appear ONLY in vencenProonto prop"
    - "Finalizado tasks appear in neither prop"
    - "Tasks with null fecha_limite are excluded from vencenProonto"
    - "Both props include eager-loaded client relationship (no N+1)"
    - "AdminLayout renders a fixed left sidebar instead of horizontal nav"
    - "All admin pages inherit the sidebar layout automatically"
  artifacts:
    - path: "app/Http/Controllers/DashboardController.php"
      provides: "Dashboard data aggregation with two queries"
      exports: ["index"]
    - path: "tests/Feature/Dashboard/DashboardTest.php"
      provides: "7 feature tests covering DASH-01 query logic"
      min_lines: 80
    - path: "resources/js/Layouts/AdminLayout.vue"
      provides: "Fixed left sidebar layout replacing horizontal nav"
      contains: "w-[220px]"
    - path: "routes/web.php"
      provides: "Dashboard route pointing to DashboardController"
      contains: "DashboardController"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/DashboardController.php"
      via: "Route::get /dashboard"
      pattern: "DashboardController::class.*index"
    - from: "app/Http/Controllers/DashboardController.php"
      to: "app/Models/Task.php"
      via: "Eloquent queries with eager loading"
      pattern: "Task::with\\('client'\\)"
---

<objective>
Create the DashboardController with two-query data aggregation (en progreso + vencen pronto), redesign AdminLayout.vue from horizontal nav to fixed left sidebar, and establish test coverage for all DASH-01 query behaviors.

Purpose: Backend data layer and layout shell must exist before the Dashboard.vue UI can be implemented in Plan 02.
Output: Working DashboardController passing 7 tests, sidebar layout visible on all admin pages, route wired.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-del-admin/07-CONTEXT.md
@.planning/phases/07-dashboard-del-admin/07-RESEARCH.md
@.planning/phases/07-dashboard-del-admin/07-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/Enums/TaskStatus.php:
```php
enum TaskStatus: string
{
    case Backlog     = 'backlog';
    case EnProgreso  = 'en_progreso';
    case EnRevision  = 'en_revision';
    case Finalizado  = 'finalizado';
}
```

From app/Enums/TaskPriority.php:
```php
enum TaskPriority: string
{
    case Baja  = 'baja';
    case Media = 'media';
    case Alta  = 'alta';
}
```

From app/Models/Task.php:
```php
class Task extends Model
{
    protected $fillable = ['titulo', 'client_id', 'descripcion', 'prioridad', 'estado', 'fecha_limite'];
    protected function casts(): array { return ['estado' => TaskStatus::class, 'prioridad' => TaskPriority::class, 'fecha_limite' => 'date']; }
    public function client(): BelongsTo { return $this->belongsTo(Client::class); }
}
```

From routes/web.php (current dashboard route — line 24):
```php
Route::get('/dashboard', function () {
    return Inertia::render('Admin/Dashboard');
})->name('dashboard');
```

From resources/js/Layouts/AdminLayout.vue (current structure):
- Horizontal `<nav>` with `<Link>` items: Dashboard, Clientes, Tareas, Facturacion, Presupuestos, Invitar Cliente
- Active state: `$page.url.startsWith('/path')` pattern
- Logout button: `router.post('/logout')`
- Main content: `<main class="mx-auto max-w-7xl py-6 px-4 sm:px-6 lg:px-8"><slot /></main>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DashboardController + feature tests + route wiring</name>
  <files>app/Http/Controllers/DashboardController.php, tests/Feature/Dashboard/DashboardTest.php, routes/web.php</files>
  <read_first>
    - app/Models/Task.php
    - app/Enums/TaskStatus.php
    - app/Enums/TaskPriority.php
    - routes/web.php
    - tests/Feature/Billing/BillingDashboardTest.php (test pattern reference)
    - database/factories/TaskFactory.php
  </read_first>
  <behavior>
    - test_dashboard_requires_admin: Unauthenticated GET /dashboard returns redirect to /login (302)
    - test_dashboard_shows_en_progreso_tasks: Create a Task with estado=en_progreso and fecha_limite=null. GET /dashboard returns Inertia component 'Admin/Dashboard' with prop 'enProgreso' containing that task
    - test_dashboard_shows_vencen_pronto_tasks: Create a Task with estado=backlog and fecha_limite=today()+3. GET /dashboard returns prop 'vencenProonto' containing that task
    - test_en_progreso_task_due_soon_appears_only_in_vencen_pronto: Create a Task with estado=en_progreso and fecha_limite=today()+2. GET /dashboard — task appears in 'vencenProonto' prop and NOT in 'enProgreso' prop
    - test_tasks_with_null_deadline_excluded_from_vencen_pronto: Create a Task with estado=en_progreso and fecha_limite=null. GET /dashboard — task appears in 'enProgreso' but NOT in 'vencenProonto'
    - test_finalizado_tasks_excluded_from_dashboard: Create a Task with estado=finalizado and fecha_limite=today()+2. GET /dashboard — task appears in neither prop
    - test_dashboard_eager_loads_client: Create a Task with estado=en_progreso. GET /dashboard — the task in 'enProgreso' has a loaded 'client' relation with 'nombre' field present
  </behavior>
  <action>
    **Step 1 — Create test file first (RED):**

    Create `tests/Feature/Dashboard/DashboardTest.php` with 7 tests using this exact structure:
    - Namespace: `Tests\Feature\Dashboard`
    - Uses `RefreshDatabase` trait
    - `setUp()` creates `$this->admin = User::factory()->create(['role' => Role::Admin])`
    - Each test uses `$this->actingAs($this->admin)->get('/dashboard')` and asserts via `assertInertia()`
    - Use `Task::factory()->create([...])` with a `Client::factory()` for the `client_id`
    - For the exclusion test (en_progreso + due soon): assert `$page->has('vencenProonto', 1)` and verify the task ID is in vencenProonto, then also assert `$page->where('enProgreso', fn($val) => collect($val)->pluck('id')->doesntContain($task->id))`
    - For the eager loading test: assert `$page->has('enProgreso.0.client')` and `$page->has('enProgreso.0.client.nombre')`
    - Use `Carbon::setTestNow(now()->startOfDay())` or `$this->travel()` is NOT needed since we use relative dates (`today()->addDays(N)`)

    Run tests: `php artisan test --filter DashboardTest` — all 7 must FAIL (no controller yet).

    **Step 2 — Create DashboardController (GREEN):**

    Create `app/Http/Controllers/DashboardController.php`:
    ```php
    namespace App\Http\Controllers;

    use App\Enums\TaskStatus;
    use App\Models\Task;
    use Inertia\Inertia;
    use Inertia\Response;

    class DashboardController extends Controller
    {
        public function index(): Response
        {
            $ventana = [today(), today()->addDays(7)];

            $vencenProonto = Task::with('client')
                ->where('estado', '!=', TaskStatus::Finalizado)
                ->whereNotNull('fecha_limite')
                ->whereBetween('fecha_limite', $ventana)
                ->orderBy('fecha_limite', 'asc')
                ->get();

            $vencenProntoIds = $vencenProonto->pluck('id');

            $enProgreso = Task::with('client')
                ->where('estado', TaskStatus::EnProgreso)
                ->whereNotIn('id', $vencenProntoIds)
                ->orderBy('created_at', 'desc')
                ->get();

            return Inertia::render('Admin/Dashboard', [
                'enProgreso'    => $enProgreso,
                'vencenProonto' => $vencenProonto,
            ]);
        }
    }
    ```

    **Step 3 — Wire route:**

    In `routes/web.php`:
    - Add `use App\Http\Controllers\DashboardController;` to imports (top of file, after existing use statements)
    - Replace the closure route on line 24-26:
      ```php
      // OLD: Route::get('/dashboard', function () { return Inertia::render('Admin/Dashboard'); })->name('dashboard');
      // NEW:
      Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
      ```

    Run tests: `php artisan test --filter DashboardTest` — all 7 must PASS.
  </action>
  <verify>
    <automated>php artisan test --filter DashboardTest</automated>
  </verify>
  <acceptance_criteria>
    - `app/Http/Controllers/DashboardController.php` exists and contains `class DashboardController`
    - `DashboardController.php` contains `Task::with('client')` (two occurrences — one per query)
    - `DashboardController.php` contains `whereNotIn('id', $vencenProntoIds)` for the exclusion rule
    - `DashboardController.php` contains `whereBetween('fecha_limite', $ventana)` for the 7-day window
    - `DashboardController.php` contains `where('estado', '!=', TaskStatus::Finalizado)` to exclude finalizado
    - `routes/web.php` contains `DashboardController::class` (not a closure for /dashboard)
    - `tests/Feature/Dashboard/DashboardTest.php` exists with 7 test methods
    - `php artisan test --filter DashboardTest` exits with code 0
  </acceptance_criteria>
  <done>DashboardController returns enProgreso and vencenProonto props with correct exclusion logic, route wired, 7 tests green</done>
</task>

<task type="auto">
  <name>Task 2: AdminLayout sidebar redesign</name>
  <files>resources/js/Layouts/AdminLayout.vue</files>
  <read_first>
    - resources/js/Layouts/AdminLayout.vue
    - .planning/phases/07-dashboard-del-admin/07-UI-SPEC.md (Layout Contract section)
    - .planning/phases/07-dashboard-del-admin/07-CONTEXT.md (sidebar decisions)
  </read_first>
  <action>
    Replace the entire `AdminLayout.vue` template from horizontal nav to fixed left sidebar. Keep the existing `<script setup>` block (imports, computed user, logout function) — only the `<template>` changes.

    **New template structure:**

    ```html
    <template>
      <div class="min-h-screen bg-gray-100 flex">
        <!-- Fixed sidebar -->
        <aside class="w-[220px] bg-white border-r border-gray-200 fixed inset-y-0 left-0 flex flex-col">
          <!-- Logo -->
          <div class="px-4 py-5 border-b border-gray-200">
            <Link href="/dashboard" class="text-xl font-bold text-gray-900">Hub</Link>
          </div>

          <!-- Nav items -->
          <nav class="flex-1 py-4 space-y-1">
            <Link href="/dashboard"
              class="flex items-center gap-3 px-4 py-3 text-sm transition"
              :class="$page.url === '/dashboard'
                ? 'bg-blue-50 text-blue-700 border-l-2 border-blue-600 font-semibold'
                : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'"
            >Dashboard</Link>

            <Link href="/clients"
              class="flex items-center gap-3 px-4 py-3 text-sm transition"
              :class="$page.url.startsWith('/clients')
                ? 'bg-blue-50 text-blue-700 border-l-2 border-blue-600 font-semibold'
                : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'"
            >Clientes</Link>

            <Link href="/tasks"
              class="flex items-center gap-3 px-4 py-3 text-sm transition"
              :class="$page.url.startsWith('/tasks')
                ? 'bg-blue-50 text-blue-700 border-l-2 border-blue-600 font-semibold'
                : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'"
            >Tareas</Link>

            <Link href="/billing"
              class="flex items-center gap-3 px-4 py-3 text-sm transition"
              :class="$page.url.startsWith('/billing')
                ? 'bg-blue-50 text-blue-700 border-l-2 border-blue-600 font-semibold'
                : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'"
            >Facturacion</Link>

            <Link href="/quotes"
              class="flex items-center gap-3 px-4 py-3 text-sm transition"
              :class="$page.url.startsWith('/quotes')
                ? 'bg-blue-50 text-blue-700 border-l-2 border-blue-600 font-semibold'
                : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'"
            >Presupuestos</Link>

            <Link href="/invitations/create"
              class="flex items-center gap-3 px-4 py-3 text-sm transition"
              :class="$page.url.startsWith('/invitations')
                ? 'bg-blue-50 text-blue-700 border-l-2 border-blue-600 font-semibold'
                : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'"
            >Invitar Cliente</Link>
          </nav>

          <!-- User + Logout at bottom -->
          <div class="border-t border-gray-200 px-4 py-4">
            <p class="text-sm text-gray-600 mb-2 truncate">{{ user?.name }}</p>
            <button
              @click="logout"
              class="text-sm text-gray-600 hover:text-gray-900 transition"
            >Cerrar sesion</button>
          </div>
        </aside>

        <!-- Main content area -->
        <div class="ml-[220px] flex-1 flex flex-col">
          <main class="p-6 flex-1">
            <slot />
          </main>
        </div>
      </div>
    </template>
    ```

    **Key details:**
    - Sidebar width: exactly `w-[220px]` (Tailwind arbitrary value)
    - Main content offset: `ml-[220px]` to prevent content from sliding under fixed sidebar
    - Active state: `bg-blue-50 text-blue-700 border-l-2 border-blue-600 font-semibold` — per UI-SPEC
    - Default state: `text-gray-600 hover:bg-gray-50 hover:text-gray-900`
    - Dashboard link uses `$page.url === '/dashboard'` (exact match since /dashboard is the only route at that prefix that matters)
    - All other links use `$page.url.startsWith('/path')` for nested route matching
    - Logo links to `/dashboard`
    - Logout at bottom of sidebar with username above it
    - No icons — text labels only (per UI-SPEC: "Icon library: none")
    - No toggle/collapse — static sidebar (locked decision)
    - The `<script setup>` block remains unchanged

    After saving, run: `php artisan test` to ensure the full test suite still passes (sidebar change is layout-only, no functional breakage expected).
  </action>
  <verify>
    <automated>php artisan test</automated>
  </verify>
  <acceptance_criteria>
    - `AdminLayout.vue` contains `w-[220px]` (sidebar width)
    - `AdminLayout.vue` contains `ml-[220px]` (main content offset)
    - `AdminLayout.vue` contains `fixed inset-y-0 left-0` (fixed positioning)
    - `AdminLayout.vue` contains `<aside` (sidebar element)
    - `AdminLayout.vue` does NOT contain `<nav class="bg-white shadow">` (old horizontal nav removed)
    - `AdminLayout.vue` contains 6 `<Link` elements: /dashboard, /clients, /tasks, /billing, /quotes, /invitations/create
    - `AdminLayout.vue` contains `bg-blue-50 text-blue-700` (active state)
    - `AdminLayout.vue` contains `Cerrar sesion` (logout button)
    - `php artisan test` exits with code 0 (full suite green)
  </acceptance_criteria>
  <done>AdminLayout.vue uses fixed left sidebar with 6 nav items, active state styling, logout at bottom, and all admin pages inherit it automatically via defineOptions</done>
</task>

</tasks>

<verification>
- `php artisan test --filter DashboardTest` — 7 tests pass (controller logic)
- `php artisan test` — full suite passes (no regressions from layout change)
- `routes/web.php` references `DashboardController::class` instead of closure
</verification>

<success_criteria>
- DashboardController exists with two-query pattern: enProgreso (excludes vencen pronto IDs) + vencenProonto (7-day window, excludes finalizado, requires non-null fecha_limite)
- Both queries use `with('client')` for eager loading
- 7 feature tests cover: auth guard, en progreso listing, vencen pronto listing, exclusion rule, null deadline exclusion, finalizado exclusion, eager loading
- AdminLayout.vue has fixed left sidebar (220px) with 6 nav items and active state
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-del-admin/07-01-SUMMARY.md`
</output>
