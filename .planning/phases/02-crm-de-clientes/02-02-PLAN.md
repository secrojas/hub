---
phase: 02-crm-de-clientes
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/Http/Controllers/ClientController.php
  - routes/web.php
  - resources/js/Pages/Admin/Clients/Index.vue
  - resources/js/Pages/Admin/Clients/Create.vue
  - resources/js/Pages/Admin/Clients/Edit.vue
  - resources/js/Pages/Admin/Clients/Show.vue
  - tests/Feature/Clients/ClientTest.php
  - tests/Feature/Clients/ClientCrudTest.php
autonomous: true
requirements:
  - CLIE-01
  - CLIE-02
  - CLIE-03
  - CLIE-04

must_haves:
  truths:
    - "El admin puede crear un cliente con nombre, email (obligatorios) y campos opcionales — redirige a lista"
    - "El admin puede ver la lista de clientes paginada (20 por pagina) con filtro por estado via dropdown"
    - "El admin puede ver la pagina de detalle de un cliente con todos los campos en modo lectura"
    - "El admin puede editar cualquier campo y los cambios persisten — email unique skip-self funciona"
    - "El admin puede eliminar un cliente via modal de confirmacion y desaparece de la lista"
    - "Estado default es activo al crear sin especificar"
  artifacts:
    - path: "app/Http/Controllers/ClientController.php"
      provides: "Resource controller con index, create, store, show, edit, update, destroy"
      exports: ["ClientController"]
    - path: "resources/js/Pages/Admin/Clients/Index.vue"
      provides: "Lista paginada con filtro estado y modal delete"
      min_lines: 80
    - path: "resources/js/Pages/Admin/Clients/Create.vue"
      provides: "Formulario creacion con todos los campos"
      min_lines: 60
    - path: "resources/js/Pages/Admin/Clients/Edit.vue"
      provides: "Formulario edicion pre-populated"
      min_lines: 60
    - path: "resources/js/Pages/Admin/Clients/Show.vue"
      provides: "Vista detalle read-only con todos los campos"
      min_lines: 40
  key_links:
    - from: "resources/js/Pages/Admin/Clients/Index.vue"
      to: "/clients?estado="
      via: "router.get with preserveState for filter"
      pattern: "router\\.get.*clients.*estado"
    - from: "resources/js/Pages/Admin/Clients/Create.vue"
      to: "/clients"
      via: "form.post for store"
      pattern: "form\\.post.*clients"
    - from: "resources/js/Pages/Admin/Clients/Edit.vue"
      to: "/clients/{id}"
      via: "form.put for update"
      pattern: "form\\.put.*clients"
    - from: "app/Http/Controllers/ClientController.php"
      to: "app/Models/Client.php"
      via: "Eloquent queries in all methods"
      pattern: "Client::"
---

<objective>
Build the complete ClientController (resource CRUD) and all four Vue pages (Index, Create, Edit, Show). Implement the test stubs from Plan 01 for CLIE-01 through CLIE-04.

Purpose: This is the core CRM functionality — admin can manage all client data.
Output: 1 controller, 4 Vue pages, route registration, 10 passing tests
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-crm-de-clientes/02-CONTEXT.md
@.planning/phases/02-crm-de-clientes/02-RESEARCH.md
@.planning/phases/02-crm-de-clientes/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — Client model created there -->
From app/Models/Client.php:
```php
class Client extends Model
{
    use HasFactory;

    protected $fillable = [
        'nombre', 'email', 'empresa', 'telefono',
        'stack_tecnologico', 'estado', 'notas', 'fecha_inicio',
    ];

    protected function casts(): array
    {
        return ['fecha_inicio' => 'date'];
    }

    public function user(): HasOne { ... }
    public function invitations(): HasMany { ... }
}
```

From routes/web.php (admin group):
```php
Route::middleware(['auth', 'admin'])->group(function () {
    Route::get('/dashboard', ...)->name('dashboard');
    Route::get('/invitations/create', ...)->name('invitations.create');
    Route::post('/invitations', ...)->name('invitations.store');
    // NEW: Route::resource('clients', ClientController::class);
});
```

From resources/js/Layouts/AdminLayout.vue:
```vue
<!-- Nav uses Link from @inertiajs/vue3, pattern: -->
<Link href="/path" class="text-sm font-medium text-gray-600 hover:text-gray-900 transition"
      :class="{ 'text-gray-900 font-semibold': $page.url.startsWith('/path') }">
    Label
</Link>
```

From resources/js/Pages/Admin/Invitations/Create.vue (pattern to follow):
```vue
<script setup>
import AdminLayout from '@/Layouts/AdminLayout.vue'
import { Head, useForm } from '@inertiajs/vue3'
import { defineOptions } from 'vue'
defineOptions({ layout: AdminLayout })
const form = useForm({ ... })
function submit() { form.post('/path', { preserveScroll: true }) }
</script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClientController + routes + Create/Edit Vue pages + CRUD tests</name>
  <read_first>
    - app/Http/Controllers/InvitationController.php (controller pattern in this project)
    - resources/js/Pages/Admin/Invitations/Create.vue (Vue form pattern)
    - routes/web.php (add resource route to admin group)
    - app/Models/Client.php (model created in Plan 01)
    - tests/Feature/Clients/ClientTest.php (stubs to implement)
  </read_first>
  <files>
    app/Http/Controllers/ClientController.php
    routes/web.php
    resources/js/Pages/Admin/Clients/Create.vue
    resources/js/Pages/Admin/Clients/Edit.vue
    tests/Feature/Clients/ClientTest.php
  </files>
  <action>
    1. Create `app/Http/Controllers/ClientController.php` — full resource controller:

       **index(Request $request):**
       - Build query with `Client::query()`
       - If `$request->filled('estado')`, add `->where('estado', $request->estado)`
       - `->latest()->paginate(20)->withQueryString()`
       - Return `Inertia::render('Admin/Clients/Index', ['clients' => $clients, 'filtroEstado' => $request->estado])`

       **create():**
       - Return `Inertia::render('Admin/Clients/Create')`

       **store(Request $request):**
       - Validate: `nombre` => required|string|max:255, `email` => required|email|unique:clients,email, `empresa` => nullable|string|max:255, `telefono` => nullable|string|max:50, `stack_tecnologico` => nullable|string, `estado` => nullable|in:activo,potencial,pausado, `notas` => nullable|string, `fecha_inicio` => nullable|date
       - Set `$data['estado'] = $data['estado'] ?? 'activo'`
       - `Client::create($data)`
       - `return redirect()->route('clients.index')`

       **show(Client $client):**
       - `$hasActiveUser = $client->user()->exists()`
       - Return `Inertia::render('Admin/Clients/Show', ['client' => $client, 'hasActiveUser' => $hasActiveUser])`

       **edit(Client $client):**
       - Return `Inertia::render('Admin/Clients/Edit', ['client' => $client])`

       **update(Request $request, Client $client):**
       - Same validation as store EXCEPT email rule: `"unique:clients,email,{$client->id}"` (skip self)
       - `$client->update($data)`
       - `return redirect()->route('clients.show', $client)`

       **destroy(Client $client):**
       - `$client->delete()`
       - `return redirect()->route('clients.index')`

    2. Add to `routes/web.php` inside the `['auth', 'admin']` group:
       ```php
       use App\Http\Controllers\ClientController;
       Route::resource('clients', ClientController::class);
       ```
       Place AFTER the existing invitations routes.

    3. Create `resources/js/Pages/Admin/Clients/Create.vue`:
       - `defineOptions({ layout: AdminLayout })`
       - useForm with all 8 fields: nombre (''), email (''), empresa (''), telefono (''), stack_tecnologico (''), estado ('activo'), notas (''), fecha_inicio ('')
       - `form.post('/clients', { preserveScroll: true })`
       - Form fields: nombre (text input), email (email input), empresa (text input), telefono (text input), stack_tecnologico (textarea), estado (select with options activo/potencial/pausado), notas (textarea), fecha_inicio (date input)
       - Each field has label + error display (`form.errors.fieldname`)
       - Submit button "Guardar" with `:disabled="form.processing"`
       - Link back to /clients "Cancelar"
       - Head title: "Nuevo Cliente"

    4. Create `resources/js/Pages/Admin/Clients/Edit.vue`:
       - `defineProps({ client: Object })`
       - `defineOptions({ layout: AdminLayout })`
       - useForm pre-populated from `props.client`: all 8 fields
       - For fecha_inicio: format date to YYYY-MM-DD string if it comes as ISO string (use `props.client.fecha_inicio?.substring(0, 10) || ''`)
       - `form.put(\`/clients/${props.client.id}\`, { preserveScroll: true })`
       - Same form layout as Create.vue
       - Head title: "Editar Cliente"
       - Link back to `/clients/${props.client.id}` "Cancelar"

    5. Implement test stubs in `tests/Feature/Clients/ClientTest.php`:

       **test_admin_can_create_client:**
       - Acting as admin, POST `/clients` with `['nombre' => 'Test Client', 'email' => 'test@example.com', 'empresa' => 'Test Corp']`
       - Assert redirect to clients.index
       - Assert DB has record in clients table with nombre='Test Client'

       **test_admin_can_update_client:**
       - Create client via factory
       - PUT `/clients/{id}` with `['nombre' => 'Updated Name', 'email' => client.email]`
       - Assert redirect
       - Assert DB has updated nombre

       **test_admin_can_delete_client:**
       - Create client via factory
       - DELETE `/clients/{id}`
       - Assert redirect to clients.index
       - Assert DB missing client record

       **test_client_stores_all_fields:**
       - POST with ALL 8 fields populated
       - Assert each field stored correctly in DB
       - Specifically check estado='potencial' when explicitly set

       **test_create_client_requires_nombre_and_email:**
       - POST `/clients` with empty data
       - Assert session has errors for 'nombre' and 'email'

       **test_create_client_rejects_duplicate_email:**
       - Create client with email X
       - POST with same email X
       - Assert session has errors for 'email'
  </action>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan test --filter=ClientTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=ClientTest` — 6 tests, 6 passed, 0 failures
    - `php artisan route:list --name=clients` shows 7 routes (index, create, store, show, edit, update, destroy)
    - `grep "unique:clients,email,{\$client->id}" app/Http/Controllers/ClientController.php` matches (skip-self on update)
    - `grep "estado.*activo.*potencial.*pausado" app/Http/Controllers/ClientController.php` matches (validation rule)
    - Create.vue has `form.post('/clients'` and all 8 form fields
    - Edit.vue has `form.put` and pre-populated fields from `props.client`
  </acceptance_criteria>
  <done>ClientController with all 7 resource methods. Create and Edit Vue pages with all form fields. 6 CRUD tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Index and Show Vue pages + list/detail tests</name>
  <read_first>
    - app/Http/Controllers/ClientController.php (index and show methods from Task 1)
    - resources/js/Pages/Admin/Clients/Create.vue (follow same Vue patterns)
    - resources/js/Layouts/AdminLayout.vue (nav link pattern for consistency)
    - tests/Feature/Clients/ClientCrudTest.php (stubs to implement)
  </read_first>
  <files>
    resources/js/Pages/Admin/Clients/Index.vue
    resources/js/Pages/Admin/Clients/Show.vue
    tests/Feature/Clients/ClientCrudTest.php
  </files>
  <action>
    1. Create `resources/js/Pages/Admin/Clients/Index.vue`:
       - `defineOptions({ layout: AdminLayout })`
       - `defineProps({ clients: Object, filtroEstado: String })`
       - Import `{ ref } from 'vue'`, `{ Head, Link, router } from '@inertiajs/vue3'`

       **Estado filter dropdown:**
       - `<select>` with options: "Todos" (value=""), "Activo", "Potencial", "Pausado"
       - Bound to local variable, default from `props.filtroEstado || ''`
       - On change: `router.get('/clients', { estado: value || undefined }, { preserveState: true, preserveScroll: true })`

       **Table:**
       - Columns: Nombre, Empresa, Estado, Fecha Inicio, Acciones
       - Iterate `clients.data` (paginated data array)
       - Nombre column: `<Link :href="\`/clients/${client.id}\`">{{ client.nombre }}</Link>` (clickable)
       - Estado: show as badge with color (activo=green, potencial=blue, pausado=gray)
       - Fecha inicio: formatted or "-" if null
       - Acciones: Link "Ver" to `/clients/{id}`, Link "Editar" to `/clients/{id}/edit`, Button "Eliminar" triggers modal

       **Pagination links:**
       - Iterate `clients.links`, render `<Link>` for each with `v-html="link.label"`, active class highlight, preserve-state

       **Delete confirmation modal:**
       - `const clienteAEliminar = ref(null)`
       - Modal visible when `clienteAEliminar` is truthy
       - Text: "Eliminar a {clienteAEliminar.nombre}?"
       - Cancel button sets `clienteAEliminar = null`
       - Confirm button: `router.delete(\`/clients/${clienteAEliminar.id}\`, { onFinish: () => clienteAEliminar.value = null })`
       - Modal overlay: `fixed inset-0 bg-black/50 flex items-center justify-center z-50`

       **Header:**
       - `<h1>Clientes</h1>` + `<Link href="/clients/create">Nuevo Cliente</Link>` button

       Head title: "Clientes"

    2. Create `resources/js/Pages/Admin/Clients/Show.vue`:
       - `defineOptions({ layout: AdminLayout })`
       - `defineProps({ client: Object, hasActiveUser: Boolean })`
       - Display ALL fields in read-only layout:
         - nombre, email, empresa (or "-"), telefono (or "-"), stack_tecnologico (or "-"), estado (badge), notas (or "-"), fecha_inicio (formatted or "-")
       - Buttons: Link "Editar" to `/clients/{id}/edit`, Link "Volver" to `/clients`
       - The "Invitar al portal" button placeholder — will be wired in Plan 03. For now, show nothing (Plan 03 adds this).
       - Head title: `client.nombre`

    3. Implement test stubs in `tests/Feature/Clients/ClientCrudTest.php`:

       **test_admin_can_view_clients_list:**
       - Create 3 clients via factory
       - GET `/clients` as admin
       - Assert Inertia component 'Admin/Clients/Index'
       - Assert Inertia has 'clients' prop with data count 3

       **test_estado_filter_returns_correct_clients:**
       - Create 2 clients with estado='activo', 1 with estado='pausado'
       - GET `/clients?estado=activo` as admin
       - Assert Inertia 'clients' prop data count is 2
       - Assert filtroEstado prop equals 'activo'

       **test_admin_can_view_client_detail:**
       - Create client via factory
       - GET `/clients/{id}` as admin
       - Assert Inertia component 'Admin/Clients/Show'
       - Assert Inertia has 'client' prop with matching id
       - Assert Inertia has 'hasActiveUser' prop (boolean)

       **test_show_page_indicates_if_client_has_active_user:**
       - Create client, create user with client_id set to that client
       - GET `/clients/{id}` as admin
       - Assert 'hasActiveUser' prop is true
  </action>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan test --filter=ClientCrudTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=ClientCrudTest` — 4 tests, 4 passed, 0 failures
    - Index.vue has `router.get('/clients'` for filter, `clients.data` iteration, `clients.links` pagination, delete modal with `clienteAEliminar`
    - Show.vue displays all 8 client fields and has `hasActiveUser` prop
    - `php artisan test` full suite green (all ClientTest + ClientCrudTest + existing)
  </acceptance_criteria>
  <done>Index page with estado filter, pagination, delete modal. Show page with all fields read-only. 4 list/detail tests passing. Full suite green.</done>
</task>

</tasks>

<verification>
- `php artisan test --filter=Client` — 10 tests passing (6 from ClientTest + 4 from ClientCrudTest), 3 incomplete (ClientInvitationTest)
- `php artisan route:list --name=clients` shows all 7 resource routes
- All 4 Vue pages render without errors: Index, Create, Edit, Show
- Filter by estado works via query param
- Delete modal confirms before destroying
- Email unique validation skips self on update
</verification>

<success_criteria>
- Admin can create client with all fields via /clients/create
- Admin can edit any field via /clients/{id}/edit — email unique skip-self works
- Admin can delete client via modal confirmation
- Admin can list clients with estado filter dropdown
- Admin can view client detail at /clients/{id}
- 10 feature tests green, 3 invitation stubs still incomplete
</success_criteria>

<output>
After completion, create `.planning/phases/02-crm-de-clientes/02-02-SUMMARY.md`
</output>
