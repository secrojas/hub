---
phase: 03-tareas-y-kanban
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - resources/js/Pages/Admin/Tasks/Index.vue
  - resources/js/Pages/Admin/Clients/Show.vue
  - resources/js/Layouts/AdminLayout.vue
  - tests/Feature/Tasks/TaskFilterTest.php
autonomous: true
requirements: [TASK-04, TASK-05]

must_haves:
  truths:
    - "Admin can filter tasks by client via dropdown"
    - "Admin can filter tasks by estado via dropdown"
    - "Admin can filter tasks by prioridad via dropdown"
    - "Admin can search tasks by titulo via text input"
    - "Multiple filters combine correctly"
    - "Link 'Ver Kanban' in client detail page navigates to /tasks?cliente={id}"
    - "AdminLayout nav includes 'Tareas' link to /tasks"
  artifacts:
    - path: "resources/js/Pages/Admin/Tasks/Index.vue"
      provides: "Filter bar with client dropdown, estado dropdown, prioridad dropdown, titulo search"
      contains: "applyFilters"
    - path: "resources/js/Pages/Admin/Clients/Show.vue"
      provides: "Ver Kanban link"
      contains: "Ver Kanban"
    - path: "resources/js/Layouts/AdminLayout.vue"
      provides: "Tareas nav link"
      contains: "/tasks"
    - path: "tests/Feature/Tasks/TaskFilterTest.php"
      provides: "4 filter tests covering TASK-05"
      contains: "test_filter_by_titulo"
  key_links:
    - from: "resources/js/Pages/Admin/Tasks/Index.vue"
      to: "app/Http/Controllers/TaskController.php"
      via: "router.get with preserveState for filters"
      pattern: "router\\.get.*tasks.*preserveState"
    - from: "resources/js/Pages/Admin/Clients/Show.vue"
      to: "resources/js/Pages/Admin/Tasks/Index.vue"
      via: "Link to /tasks?cliente={id}"
      pattern: "href.*tasks.*cliente"
    - from: "resources/js/Layouts/AdminLayout.vue"
      to: "resources/js/Pages/Admin/Tasks/Index.vue"
      via: "Nav link to /tasks"
      pattern: "href.*\\/tasks"
---

<objective>
Add filter/search UI to the Kanban board, wire the "Ver Kanban" link in client detail, add "Tareas" nav item, and implement all filter tests.

Purpose: Completes TASK-04 (global view with client names visible) and TASK-05 (filter by titulo, estado, prioridad). Also integrates the Kanban into the existing navigation flow.
Output: Fully filterable Kanban, nav link, client-to-kanban link, 4 passing filter tests.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tareas-y-kanban/03-RESEARCH.md
@.planning/phases/03-tareas-y-kanban/03-02-SUMMARY.md

@resources/js/Pages/Admin/Tasks/Index.vue
@resources/js/Pages/Admin/Clients/Show.vue
@resources/js/Layouts/AdminLayout.vue
@app/Http/Controllers/TaskController.php
@tests/Feature/Tasks/TaskFilterTest.php

<interfaces>
<!-- From TaskController.php (Plan 02) -->

Props passed to Admin/Tasks/Index.vue:
```javascript
// columns: { backlog: Task[], en_progreso: Task[], en_revision: Task[], finalizado: Task[] }
// clients: { id: number, nombre: string }[]
// filtros: { cliente?: string, estado?: string, prioridad?: string, titulo?: string }
```

Route: GET /tasks accepts query params: ?cliente={id}&estado={value}&prioridad={value}&titulo={search}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter bar to Kanban Index.vue, "Ver Kanban" link in Show.vue, "Tareas" nav in AdminLayout</name>
  <files>
    resources/js/Pages/Admin/Tasks/Index.vue,
    resources/js/Pages/Admin/Clients/Show.vue,
    resources/js/Layouts/AdminLayout.vue
  </files>
  <read_first>
    resources/js/Pages/Admin/Tasks/Index.vue,
    resources/js/Pages/Admin/Clients/Show.vue,
    resources/js/Pages/Admin/Clients/Index.vue,
    resources/js/Layouts/AdminLayout.vue,
    app/Http/Controllers/TaskController.php
  </read_first>
  <action>
    1. **Update `resources/js/Pages/Admin/Tasks/Index.vue`** — Add filter bar ABOVE the Kanban columns:

       **Filter state:** Initialize from `props.filtros` prop:
       ```javascript
       const filters = ref({
           cliente:   props.filtros?.cliente ?? '',
           estado:    props.filtros?.estado ?? '',
           prioridad: props.filtros?.prioridad ?? '',
           titulo:    props.filtros?.titulo ?? '',
       })
       ```

       **applyFilters function:** Uses `router.get` with `preserveState: true, preserveScroll: true, replace: true`. `replace: true` prevents browser history pollution from typing in search.
       ```javascript
       function applyFilters() {
           // Strip empty values so URL stays clean
           const params = Object.fromEntries(
               Object.entries(filters.value).filter(([_, v]) => v !== '' && v !== null)
           )
           router.get('/tasks', params, {
               preserveState: true,
               preserveScroll: true,
               replace: true,
           })
       }
       ```

       **Filter bar template** (above the 4-column flex):
       ```html
       <div class="mb-6 flex flex-wrap gap-3 items-end">
           <!-- Client dropdown -->
           <div>
               <label class="block text-xs font-medium text-gray-500 mb-1">Cliente</label>
               <select v-model="filters.cliente" @change="applyFilters" class="border-gray-300 rounded text-sm">
                   <option value="">Todos</option>
                   <option v-for="c in clients" :key="c.id" :value="c.id">{{ c.nombre }}</option>
               </select>
           </div>
           <!-- Estado dropdown -->
           <div>
               <label class="block text-xs font-medium text-gray-500 mb-1">Estado</label>
               <select v-model="filters.estado" @change="applyFilters" class="border-gray-300 rounded text-sm">
                   <option value="">Todos</option>
                   <option value="backlog">Backlog</option>
                   <option value="en_progreso">En progreso</option>
                   <option value="en_revision">En revision</option>
                   <option value="finalizado">Finalizado</option>
               </select>
           </div>
           <!-- Prioridad dropdown -->
           <div>
               <label class="block text-xs font-medium text-gray-500 mb-1">Prioridad</label>
               <select v-model="filters.prioridad" @change="applyFilters" class="border-gray-300 rounded text-sm">
                   <option value="">Todas</option>
                   <option value="alta">Alta</option>
                   <option value="media">Media</option>
                   <option value="baja">Baja</option>
               </select>
           </div>
           <!-- Titulo search -->
           <div>
               <label class="block text-xs font-medium text-gray-500 mb-1">Buscar</label>
               <input
                   v-model="filters.titulo"
                   @input="applyFilters"
                   type="text"
                   placeholder="Buscar por titulo..."
                   class="border-gray-300 rounded text-sm w-48"
               />
           </div>
       </div>
       ```

       Consider debouncing the titulo @input (300ms) to avoid excessive requests. Use a simple setTimeout debounce:
       ```javascript
       let searchTimeout = null
       function onTituloInput() {
           clearTimeout(searchTimeout)
           searchTimeout = setTimeout(applyFilters, 300)
       }
       ```
       Bind `@input="onTituloInput"` on the titulo input instead of `@input="applyFilters"`.

    2. **Update `resources/js/Pages/Admin/Clients/Show.vue`** — Add "Ver Kanban" link:
       After the "Portal Invitation Section" div (or within the action buttons area at the top), add a link:
       ```html
       <Link
           :href="`/tasks?cliente=${client.id}`"
           class="px-4 py-2 bg-emerald-600 text-white rounded hover:bg-emerald-700 text-sm font-medium"
       >
           Ver Kanban
       </Link>
       ```
       Place it in the `flex gap-3` div alongside the "Editar" and "Volver" buttons.

    3. **Update `resources/js/Layouts/AdminLayout.vue`** — Add "Tareas" nav link:
       After the "Clientes" link and before the "Invitar Cliente" link, add:
       ```html
       <Link
           href="/tasks"
           class="text-sm font-medium text-gray-600 hover:text-gray-900 transition"
           :class="{ 'text-gray-900 font-semibold': $page.url.startsWith('/tasks') }"
       >
           Tareas
       </Link>
       ```

    4. Run `npm run build` to verify compilation.
  </action>
  <acceptance_criteria>
    - grep: `applyFilters` in resources/js/Pages/Admin/Tasks/Index.vue
    - grep: `router.get.*tasks\|router\.get.*\/tasks` in resources/js/Pages/Admin/Tasks/Index.vue
    - grep: `preserveState.*true` in resources/js/Pages/Admin/Tasks/Index.vue (for filter calls)
    - grep: `replace.*true` in resources/js/Pages/Admin/Tasks/Index.vue
    - grep: `filters.cliente\|v-model.*filters.cliente` in resources/js/Pages/Admin/Tasks/Index.vue
    - grep: `filters.titulo\|v-model.*filters.titulo` in resources/js/Pages/Admin/Tasks/Index.vue
    - grep: `filters.estado\|v-model.*filters.estado` in resources/js/Pages/Admin/Tasks/Index.vue
    - grep: `filters.prioridad\|v-model.*filters.prioridad` in resources/js/Pages/Admin/Tasks/Index.vue
    - grep: `Ver Kanban` in resources/js/Pages/Admin/Clients/Show.vue
    - grep: `tasks.*cliente` in resources/js/Pages/Admin/Clients/Show.vue
    - grep: `href.*\/tasks` in resources/js/Layouts/AdminLayout.vue
    - grep: `Tareas` in resources/js/Layouts/AdminLayout.vue
  </acceptance_criteria>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Filter bar with 4 inputs (client dropdown, estado dropdown, prioridad dropdown, titulo search) works with router.get preserveState. "Ver Kanban" link in Show.vue navigates to /tasks?cliente={id}. "Tareas" nav link in AdminLayout. npm build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Implement filter test stubs and run full test suite</name>
  <files>
    tests/Feature/Tasks/TaskFilterTest.php
  </files>
  <read_first>
    tests/Feature/Tasks/TaskFilterTest.php,
    tests/Feature/Tasks/TaskKanbanTest.php,
    app/Http/Controllers/TaskController.php
  </read_first>
  <action>
    1. Implement all 4 test stubs in `tests/Feature/Tasks/TaskFilterTest.php` — remove `markTestIncomplete()`:

       **Setup (setUp or inline):** Create admin user, actingAs admin. Create 2 clients. Create tasks with known values for filtering.

       **test_filter_by_cliente:**
       - Create client A with 2 tasks, client B with 1 task
       - GET /tasks?cliente={clientA->id}
       - Assert Inertia component 'Admin/Tasks/Index'
       - Assert columns contain only client A's tasks (count total across all columns = 2)
       - Assert client B's task is NOT in any column

       **test_filter_by_estado:**
       - Create 1 task with estado=backlog, 1 task with estado=en_progreso
       - GET /tasks?estado=en_progreso
       - Assert only the en_progreso task appears in columns
       - The backlog task should NOT appear

       **test_filter_by_titulo:**
       - Create task with titulo='Disenar landing page', another with titulo='Configurar servidor'
       - GET /tasks?titulo=landing
       - Assert only the 'Disenar landing page' task appears
       - Assert 'Configurar servidor' does NOT appear

       **test_filter_by_prioridad:**
       - Create 1 task with prioridad=alta, 1 with prioridad=baja
       - GET /tasks?prioridad=alta
       - Assert only the alta task appears

       For all tests: extract tasks from Inertia props `columns` (merge all 4 column arrays), then assert counts and presence/absence.

    2. Run `php artisan test --filter=TaskFilter` to verify all 4 pass.
    3. Run `php artisan test` to verify full suite green (existing 53+ tests + new 12 tests).
  </action>
  <acceptance_criteria>
    - grep: `assertInertia\|assertOk\|get.*tasks` in tests/Feature/Tasks/TaskFilterTest.php
    - grep: `cliente.*client.*id\|cliente.*=>` in tests/Feature/Tasks/TaskFilterTest.php
    - grep: `titulo.*landing\|titulo.*=>` in tests/Feature/Tasks/TaskFilterTest.php
    - grep: `prioridad.*alta` in tests/Feature/Tasks/TaskFilterTest.php
    - grep: `estado.*en_progreso` in tests/Feature/Tasks/TaskFilterTest.php
    - NO grep: `markTestIncomplete` in tests/Feature/Tasks/TaskFilterTest.php
  </acceptance_criteria>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan test --filter=TaskFilter 2>&1 | tail -5 && php artisan test 2>&1 | tail -5</automated>
  </verify>
  <done>4 filter tests pass (by client, by estado, by titulo, by prioridad). Full test suite green. No incomplete test stubs remain in any Task test file.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `php artisan test --filter=TaskFilter` — 4 tests pass
- `php artisan test --filter=Task` — all 12 tests pass (4 CRUD + 4 Kanban + 4 Filter)
- `php artisan test` — full suite green
- GET /tasks shows filter bar + 4 Kanban columns
- GET /tasks?cliente=1 scopes to that client's tasks
- Show.vue has "Ver Kanban" link
- AdminLayout has "Tareas" nav link
</verification>

<success_criteria>
- Filter bar on Kanban page with client dropdown, estado dropdown, prioridad dropdown, titulo search
- Filters use router.get with preserveState + preserveScroll + replace
- Titulo search debounced (300ms)
- "Ver Kanban" link on client detail navigates to /tasks?cliente={id}
- "Tareas" nav link in AdminLayout highlights when on /tasks
- 4 filter tests pass
- All 12 task tests pass
- Full suite green
- npm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-tareas-y-kanban/03-03-SUMMARY.md`
</output>
