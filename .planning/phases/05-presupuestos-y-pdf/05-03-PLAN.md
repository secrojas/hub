---
phase: 05-presupuestos-y-pdf
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/Http/Controllers/QuoteController.php
  - resources/views/pdf/quote.blade.php
  - tests/Feature/QuoteTest.php
autonomous: false
requirements:
  - QUOT-03

must_haves:
  truths:
    - "GET /quotes/{id}/pdf returns a PDF download for non-borrador quotes"
    - "GET /quotes/{id}/pdf returns 403 for borrador quotes"
    - "PDF response has Content-Type application/pdf"
    - "PDF filename follows pattern presupuesto-{id}-{slug}.pdf"
    - "PDF contains srojasweb header, client data, items table with ARS formatting, and total"
    - "Spanish characters (ñ, á, é) render correctly in PDF via DejaVu Sans font"
  artifacts:
    - path: "resources/views/pdf/quote.blade.php"
      provides: "Blade template for PDF rendering with UTF-8 charset and DejaVu Sans"
      contains: "meta charset=\"utf-8\""
    - path: "app/Http/Controllers/QuoteController.php"
      provides: "pdf() method using Pdf::loadView and streamDownload"
      contains: "Pdf::loadView"
  key_links:
    - from: "app/Http/Controllers/QuoteController.php"
      to: "resources/views/pdf/quote.blade.php"
      via: "Pdf::loadView('pdf.quote')"
      pattern: "Pdf::loadView\\('pdf\\.quote'"
    - from: "resources/views/pdf/quote.blade.php"
      to: "app/Models/Quote.php"
      via: "$quote variable with loaded client and items"
      pattern: "\\$quote->items"
---

<objective>
Implement the PDF generation feature: Blade template for the presupuesto PDF, QuoteController@pdf method using dompdf, and complete the 3 remaining QUOT-03 test stubs. Visual verification of PDF output with Spanish characters and ARS formatting.

Purpose: Complete the PDF download capability — the final requirement for Phase 5.
Output: Working PDF download at /quotes/{id}/pdf, 3 tests passing, visual confirmation of PDF quality.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-presupuestos-y-pdf/05-CONTEXT.md
@.planning/phases/05-presupuestos-y-pdf/05-RESEARCH.md
@.planning/phases/05-presupuestos-y-pdf/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From app/Enums/QuoteStatus.php:
```php
enum QuoteStatus: string
{
    case Borrador  = 'borrador';
    case Enviado   = 'enviado';
    case Aceptado  = 'aceptado';
    case Rechazado = 'rechazado';
}
```

From app/Models/Quote.php:
```php
public function client(): BelongsTo { return $this->belongsTo(Client::class); }
public function items(): HasMany { return $this->hasMany(QuoteItem::class); }
// casts: 'estado' => QuoteStatus::class
```

From app/Models/QuoteItem.php:
```php
// casts: 'precio' => 'decimal:2'
```

<!-- dompdf usage pattern from RESEARCH.md -->
```php
use Barryvdh\DomPDF\Facade\Pdf;
use Illuminate\Support\Str;

$pdf = Pdf::loadView('pdf.quote', ['quote' => $quote]);
$slug = Str::slug($quote->titulo);
$filename = "presupuesto-{$quote->id}-{$slug}.pdf";
return response()->streamDownload(
    fn () => print($pdf->output()),
    $filename,
    ['Content-Type' => 'application/pdf']
);
```

<!-- Routes already registered in Plan 02 -->
```php
Route::get('quotes/{quote}/pdf', [QuoteController::class, 'pdf'])->name('quotes.pdf');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blade PDF template, QuoteController@pdf method, and QUOT-03 tests</name>
  <files>
    resources/views/pdf/quote.blade.php,
    app/Http/Controllers/QuoteController.php,
    tests/Feature/QuoteTest.php
  </files>
  <read_first>
    app/Http/Controllers/QuoteController.php,
    app/Enums/QuoteStatus.php,
    app/Models/Quote.php,
    app/Models/QuoteItem.php,
    tests/Feature/QuoteTest.php,
    .planning/phases/05-presupuestos-y-pdf/05-RESEARCH.md
  </read_first>
  <action>
1. Create directory `resources/views/pdf/` if it does not exist.

2. Create `resources/views/pdf/quote.blade.php` — the HTML template for dompdf:
   ```html
   <!DOCTYPE html>
   <html>
   <head>
       <meta charset="utf-8">
       <style>
           body { font-family: DejaVu Sans, sans-serif; font-size: 12px; color: #333; margin: 40px; }
           h1 { color: #1f2937; margin-bottom: 4px; }
           .subtitle { color: #6b7280; font-size: 11px; margin-bottom: 24px; }
           .client-info { margin-bottom: 20px; }
           .client-info p { margin: 2px 0; }
           table { width: 100%; border-collapse: collapse; margin-top: 16px; }
           th { background: #f3f4f6; text-align: left; padding: 8px; border-bottom: 2px solid #e5e7eb; }
           td { padding: 8px; border-bottom: 1px solid #e5e7eb; }
           .text-right { text-align: right; }
           .total { font-weight: bold; font-size: 14px; }
           .notes { margin-top: 24px; padding: 12px; background: #f9fafb; border-left: 4px solid #d1d5db; }
           .estado-badge { display: inline-block; padding: 2px 8px; border-radius: 4px; font-size: 11px; }
       </style>
   </head>
   <body>
       <h1>srojasweb</h1>
       <p class="subtitle">Fecha: {{ $quote->created_at->format('d/m/Y') }}</p>

       <h2>{{ $quote->titulo }}</h2>
       <p>Estado: {{ ucfirst($quote->estado->value) }}</p>

       <div class="client-info">
           <p><strong>Cliente:</strong> {{ $quote->client->nombre }}</p>
           <p><strong>Empresa:</strong> {{ $quote->client->empresa }}</p>
           <p><strong>Email:</strong> {{ $quote->client->email }}</p>
       </div>

       <table>
           <thead>
               <tr>
                   <th>Descripcion</th>
                   <th class="text-right">Precio</th>
               </tr>
           </thead>
           <tbody>
               @foreach($quote->items as $item)
               <tr>
                   <td>{{ $item->descripcion }}</td>
                   <td class="text-right">$ {{ number_format($item->precio, 2, ',', '.') }}</td>
               </tr>
               @endforeach
           </tbody>
           <tfoot>
               <tr>
                   <td class="total text-right">Total</td>
                   <td class="total text-right">$ {{ number_format($quote->items->sum('precio'), 2, ',', '.') }}</td>
               </tr>
           </tfoot>
       </table>

       @if($quote->notas)
       <div class="notes">
           <strong>Notas:</strong><br>{{ $quote->notas }}
       </div>
       @endif
   </body>
   </html>
   ```

   CRITICAL: `font-family: DejaVu Sans` is mandatory — it is bundled with dompdf and has full UTF-8/Latin support. The default font does NOT render n with tilde, a with accent, e with accent correctly.

3. Add `pdf()` method to `app/Http/Controllers/QuoteController.php`:
   ```php
   use Barryvdh\DomPDF\Facade\Pdf;
   use Illuminate\Support\Str;

   public function pdf(Quote $quote)
   {
       abort_if($quote->estado === QuoteStatus::Borrador, 403);

       $quote->load(['client', 'items']);

       $pdf = Pdf::loadView('pdf.quote', ['quote' => $quote]);

       $slug = Str::slug($quote->titulo);
       $filename = "presupuesto-{$quote->id}-{$slug}.pdf";

       return response()->streamDownload(
           fn () => print($pdf->output()),
           $filename,
           ['Content-Type' => 'application/pdf']
       );
   }
   ```

4. Implement the 3 remaining QUOT-03 test stubs in `tests/Feature/QuoteTest.php`:

   **test_pdf_downloads_for_enviado_quote:**
   - Create quote with `QuoteFactory::new()->enviado()->create()`, add 2 items via `QuoteItem::factory()->for($quote)->count(2)->create()`
   - GET `/quotes/{id}/pdf`
   - `assertStatus(200)`
   - `assertHeader('Content-Type', 'application/pdf')`

   **test_pdf_forbidden_for_borrador_quote:**
   - Create borrador quote
   - GET `/quotes/{id}/pdf`
   - `assertStatus(403)`

   **test_pdf_response_has_correct_filename:**
   - Create enviado quote with `titulo` = 'Landing Page Empresa XYZ', add items
   - GET `/quotes/{id}/pdf`
   - `assertStatus(200)`
   - Assert response header `Content-Disposition` contains `presupuesto-{$quote->id}-landing-page-empresa-xyz.pdf`
  </action>
  <verify>
    <automated>php artisan test --filter QuoteTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - resources/views/pdf/quote.blade.php contains 'meta charset="utf-8"' and 'DejaVu Sans' and 'srojasweb' and 'number_format' and '$quote->items'
    - app/Http/Controllers/QuoteController.php contains "use Barryvdh\DomPDF\Facade\Pdf" and "Pdf::loadView('pdf.quote'" and "streamDownload" and "abort_if($quote->estado === QuoteStatus::Borrador, 403)"
    - app/Http/Controllers/QuoteController.php contains "Str::slug($quote->titulo)" and "presupuesto-{$quote->id}-{$slug}.pdf"
    - tests/Feature/QuoteTest.php does NOT contain "markTestIncomplete" (all 13 stubs are now implemented)
    - php artisan test --filter QuoteTest shows 13 passing, 0 incomplete, 0 failures
  </acceptance_criteria>
  <done>PDF downloads correctly for non-borrador quotes, 403 for borrador, filename follows pattern, all 13 tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of PDF output</name>
  <files>resources/views/pdf/quote.blade.php</files>
  <action>Human visually verifies the PDF output. No code changes needed — this is a visual inspection checkpoint after Task 1 automation is complete.</action>
  <what-built>PDF generation for presupuestos via barryvdh/laravel-dompdf — sync download at /quotes/{id}/pdf with Spanish characters, ARS formatting, client data, items table, and total.</what-built>
  <how-to-verify>
    1. Start dev server: `php artisan serve` (if not running)
    2. Navigate to http://localhost:8000/quotes
    3. Create a new presupuesto:
       - Select any client
       - Titulo: "Diseno Landing Page" (use accented characters to test)
       - Add 2 items: "Diseno UI/UX" at 25000, "Desarrollo Frontend" at 50000
       - Notas: "Incluye revision final"
       - Save
    4. From the quotes list, change estado to "Enviado" using the inline select
    5. Click "Descargar PDF" — a PDF should download
    6. Open the PDF and verify:
       - Header says "srojasweb" with the creation date
       - Client name, empresa, and email appear
       - Title shows correctly with any special characters
       - Items table shows both items with prices in ARS format: "$ 25.000,00" and "$ 50.000,00"
       - Total shows "$ 75.000,00" in bold
       - Notas section appears at the bottom
       - Filename follows pattern "presupuesto-{id}-{slug}.pdf"
    7. Verify the "Descargar PDF" button does NOT appear for borrador quotes
    8. Verify the "Eliminar" button does NOT appear for enviado quotes
  </how-to-verify>
  <verify>Human confirms PDF renders correctly with Spanish characters and ARS formatting</verify>
  <done>PDF visually confirmed: Spanish chars render, ARS amounts formatted correctly, all sections present.</done>
  <resume-signal>Type "approved" if PDF renders correctly, or describe any issues with characters, formatting, or layout.</resume-signal>
</task>

</tasks>

<verification>
- `php artisan test --filter QuoteTest` — 13 tests pass, 0 failures
- `php artisan test` — full suite green
- PDF visually verified: Spanish chars, ARS format, client data, items, total, notas
</verification>

<success_criteria>
- PDF downloads with correct Content-Type and filename pattern
- PDF blocked (403) for borrador quotes
- PDF contains: srojasweb header, client info, titulo, estado, items table with ARS formatting, total in bold, notas if present
- Spanish characters render without corruption
- ARS amounts use comma as decimal separator and period as thousands separator
- All 13 QuoteTest methods pass
- Human has visually confirmed PDF quality
</success_criteria>

<output>
After completion, create `.planning/phases/05-presupuestos-y-pdf/05-03-SUMMARY.md`
</output>
