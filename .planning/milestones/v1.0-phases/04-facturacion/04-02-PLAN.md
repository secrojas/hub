---
phase: 04-facturacion
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/Http/Controllers/BillingController.php
  - app/Http/Requests/StoreBillingRequest.php
  - app/Http/Requests/UpdateBillingRequest.php
  - resources/js/Pages/Admin/Billing/Index.vue
  - resources/js/Pages/Admin/Billing/Create.vue
  - resources/js/Pages/Admin/Billing/Edit.vue
  - routes/web.php
  - tests/Feature/Billing/BillingCrudTest.php
  - tests/Feature/Billing/BillingValidationTest.php
autonomous: true
requirements:
  - BILL-01
  - BILL-02

must_haves:
  truths:
    - "Admin can create a billing record with client, concepto, monto, fecha_emision, fecha_pago, estado"
    - "Admin can update an existing billing record"
    - "Admin can delete a billing record with confirmation modal"
    - "fecha_pago is required when estado=pagado, rejected if missing"
    - "fecha_pago initialized as null (not empty string) in useForm"
    - "estado only accepts pendiente/pagado/vencido"
  artifacts:
    - path: "app/Http/Controllers/BillingController.php"
      provides: "CRUD controller (index, create, store, edit, update, destroy)"
      exports: ["index", "create", "store", "edit", "update", "destroy"]
    - path: "app/Http/Requests/StoreBillingRequest.php"
      provides: "Store validation with required_if:estado,pagado"
      contains: "required_if:estado,pagado"
    - path: "app/Http/Requests/UpdateBillingRequest.php"
      provides: "Update validation with required_if:estado,pagado"
      contains: "required_if:estado,pagado"
    - path: "resources/js/Pages/Admin/Billing/Create.vue"
      provides: "Create billing form page"
      contains: "fecha_pago: null"
    - path: "resources/js/Pages/Admin/Billing/Edit.vue"
      provides: "Edit billing form + delete modal"
      contains: "fecha_pago"
    - path: "resources/js/Pages/Admin/Billing/Index.vue"
      provides: "Billing table with pagination"
      contains: "billings"
    - path: "routes/web.php"
      provides: "Billing resource routes"
      contains: "BillingController"
  key_links:
    - from: "resources/js/Pages/Admin/Billing/Create.vue"
      to: "app/Http/Controllers/BillingController.php"
      via: "form.post('/billing')"
      pattern: "post.*billing"
    - from: "app/Http/Controllers/BillingController.php"
      to: "app/Http/Requests/StoreBillingRequest.php"
      via: "type-hinted Form Request"
      pattern: "StoreBillingRequest"
    - from: "routes/web.php"
      to: "app/Http/Controllers/BillingController.php"
      via: "Route::resource"
      pattern: "Route::resource.*billing.*BillingController"
---

<objective>
Build the billing CRUD: controller, Form Requests, Vue pages, routes, and implement test stubs.

Purpose: Deliver the core billing management functionality — admin can create, view, edit, and delete billing records with proper validation including the conditional fecha_pago requirement.
Output: Working BillingController with 6 actions, StoreBillingRequest/UpdateBillingRequest with required_if, three Vue pages (Index/Create/Edit), routes, and green CRUD + validation tests.
</objective>

<execution_context>
@C:/Users/sroja/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sroja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-facturacion/04-CONTEXT.md
@.planning/phases/04-facturacion/04-RESEARCH.md
@.planning/phases/04-facturacion/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From app/Models/Billing.php:
```php
class Billing extends Model
{
    use HasFactory;
    protected $fillable = ['client_id', 'concepto', 'monto', 'fecha_emision', 'fecha_pago', 'estado'];
    // casts: estado => BillingStatus::class, fecha_emision => date, fecha_pago => date, monto => decimal:2
    public function client(): BelongsTo;
}
```

From app/Enums/BillingStatus.php:
```php
enum BillingStatus: string
{
    case Pendiente = 'pendiente';
    case Pagado = 'pagado';
    case Vencido = 'vencido';
}
```

From app/Http/Controllers/TaskController.php (filter pattern to mirror):
```php
public function index(Request $request)
{
    $query = Task::with('client')
        ->when($request->filled('cliente'), fn ($q) => $q->where('client_id', $request->cliente))
        ->when($request->filled('estado'), fn ($q) => $q->where('estado', $request->estado))
        ->latest()->get();
    // ...
}
```

From routes/web.php (add billing routes inside admin group):
```php
Route::middleware(['auth', 'admin'])->group(function () {
    // ... existing routes
    Route::resource('tasks', TaskController::class)->except(['show', 'create', 'edit']);
    Route::put('tasks/{task}/status', [TaskController::class, 'updateStatus'])->name('tasks.updateStatus');
    // ADD billing route here
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BillingController + Form Requests + routes + CRUD tests</name>
  <files>
    app/Http/Controllers/BillingController.php,
    app/Http/Requests/StoreBillingRequest.php,
    app/Http/Requests/UpdateBillingRequest.php,
    routes/web.php,
    tests/Feature/Billing/BillingCrudTest.php,
    tests/Feature/Billing/BillingValidationTest.php
  </files>
  <read_first>
    app/Http/Controllers/TaskController.php,
    app/Http/Controllers/ClientController.php,
    app/Http/Requests/StoreTaskRequest.php,
    routes/web.php,
    tests/Feature/Billing/BillingCrudTest.php,
    tests/Feature/Billing/BillingValidationTest.php,
    tests/Feature/Tasks/TaskCrudTest.php
  </read_first>
  <action>
1. Create `app/Http/Requests/StoreBillingRequest.php`:
   - `authorize()` returns true
   - `rules()`:
     - client_id: required, exists:clients,id
     - concepto: required, string, max:255
     - monto: required, numeric, min:0.01
     - fecha_emision: required, date
     - fecha_pago: nullable, date, required_if:estado,pagado  -- CRITICAL: required_if rule
     - estado: required, in:pendiente,pagado,vencido

2. Create `app/Http/Requests/UpdateBillingRequest.php` — identical rules to Store (no unique constraints on billings).

3. Create `app/Http/Controllers/BillingController.php` with 6 methods:
   - `index(Request $request)`: Query billings with('client'), filter by `$request->cliente` (client_id) and `$request->estado`, `->latest()->paginate(20)->withQueryString()`. Build summary array with (float) casts:
     - cobrado_mes: `(float) Billing::where('estado','pagado')->whereMonth('fecha_pago',now()->month)->whereYear('fecha_pago',now()->year)->sum('monto')`
     - pendiente_total: `(float) Billing::where('estado','pendiente')->sum('monto')`
     - vencidos_count: `Billing::where('estado','vencido')->count()`
     CRITICAL: Cast sum() results with (float) to avoid "0" string issue.
     Return Inertia::render('Admin/Billing/Index', [billings, clients (id+nombre for dropdown), filtros, summary])
   - `create()`: Return Inertia::render('Admin/Billing/Create', [clients => Client::orderBy('nombre')->get(['id','nombre'])])
   - `store(StoreBillingRequest $request)`: Billing::create($request->validated()), redirect to billing.index
   - `edit(Billing $billing)`: Load $billing->load('client'), Return Inertia::render('Admin/Billing/Edit', [billing, clients])
   - `update(UpdateBillingRequest $request, Billing $billing)`: $billing->update($request->validated()), redirect to billing.index
   - `destroy(Billing $billing)`: $billing->delete(), redirect to billing.index

4. Add route in `routes/web.php` inside the `['auth', 'admin']` middleware group, after tasks routes:
   `Route::resource('billing', BillingController::class)->except(['show']);`
   Add `use App\Http\Controllers\BillingController;` import at top.

5. Implement test stubs in `tests/Feature/Billing/BillingCrudTest.php` (replace markTestIncomplete):
   - test_admin_can_view_billing_index: create 3 billings, GET /billing, assertInertia has 'billings', assert billings.data count 3
   - test_admin_can_create_billing: POST /billing with valid data, assertRedirect, assertDatabaseHas
   - test_admin_can_update_billing: create billing, PUT /billing/{id} with changed concepto, assertDatabaseHas new value
   - test_admin_can_delete_billing: create billing, DELETE /billing/{id}, assertDatabaseMissing
   - test_guest_cannot_access_billing: unauthenticated GET /billing, assertRedirect to login

6. Implement test stubs in `tests/Feature/Billing/BillingValidationTest.php`:
   - test_store_requires_concepto_monto_fecha_emision_estado: POST /billing with empty data, assertSessionHasErrors for each required field
   - test_fecha_pago_required_when_estado_is_pagado: POST /billing with estado=pagado and fecha_pago omitted, assertSessionHasErrors('fecha_pago')
   - test_fecha_pago_not_required_when_estado_is_pendiente: POST /billing with estado=pendiente and fecha_pago=null, should NOT have fecha_pago error
   - test_estado_rejects_invalid_values: POST /billing with estado='invalido', assertSessionHasErrors('estado')
  </action>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && php artisan test --filter=BillingCrudTest --filter=BillingValidationTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "required_if:estado,pagado" app/Http/Requests/StoreBillingRequest.php
    - grep -q "required_if:estado,pagado" app/Http/Requests/UpdateBillingRequest.php
    - grep -q "(float)" app/Http/Controllers/BillingController.php
    - grep -q "nullOnDelete\|BillingController" routes/web.php
    - grep -q "Route::resource.*billing.*BillingController" routes/web.php
    - grep -q "paginate(20)" app/Http/Controllers/BillingController.php
    - grep -rL "markTestIncomplete" tests/Feature/Billing/BillingCrudTest.php (should find the file, meaning no stubs remain)
    - grep -rL "markTestIncomplete" tests/Feature/Billing/BillingValidationTest.php
  </acceptance_criteria>
  <done>9 tests green (5 CRUD + 4 validation), BillingController CRUD works, required_if validation enforced, routes registered</done>
</task>

<task type="auto">
  <name>Task 2: Index/Create/Edit Vue pages with forms and delete modal</name>
  <files>
    resources/js/Pages/Admin/Billing/Index.vue,
    resources/js/Pages/Admin/Billing/Create.vue,
    resources/js/Pages/Admin/Billing/Edit.vue
  </files>
  <read_first>
    resources/js/Pages/Admin/Tasks/Index.vue,
    resources/js/Pages/Admin/Clients/Create.vue,
    resources/js/Pages/Admin/Clients/Edit.vue,
    resources/js/Pages/Admin/Clients/Index.vue,
    resources/js/Layouts/AdminLayout.vue
  </read_first>
  <action>
1. Create `resources/js/Pages/Admin/Billing/Index.vue`:
   - `defineOptions({ layout: AdminLayout })`
   - Props: billings (paginated), clients (array), filtros (object), summary (object)
   - NO summary cards yet (Plan 03 handles dashboard display). Just render the billing table and filter bar.
   - Filter bar: estado dropdown (Todos/Pendiente/Pagado/Vencido) + cliente dropdown (Todos + clients list). Use `router.get('/billing', filters, { preserveState: true, preserveScroll: true })` pattern from Tasks.
   - Table columns: Concepto, Cliente (billing.client?.nombre or "Sin cliente"), Monto (formatted with Intl.NumberFormat('es-AR', { style:'currency', currency:'ARS' })), Fecha Emision, Estado (badge), Acciones (Editar link)
   - Pagination: Render `billings.links` with Link components (same pattern as Clients/Index.vue)
   - "Nuevo Cobro" button linking to /billing/create
   - Delete button per row with ref(null) sentinel modal pattern (from Phase 2)

2. Create `resources/js/Pages/Admin/Billing/Create.vue`:
   - `defineOptions({ layout: AdminLayout })`
   - Props: clients (array for dropdown)
   - useForm with fields: client_id: '', concepto: '', monto: '', fecha_emision: '', fecha_pago: null, estado: 'pendiente'
   - CRITICAL: `fecha_pago: null` — NOT empty string. This ensures required_if validation works correctly.
   - Form fields: client dropdown, concepto text input, monto text input (type="text", NOT type="number" — placeholder "1500.50"), fecha_emision date input, fecha_pago date input, estado select (pendiente/pagado/vencido)
   - Show validation errors per field (form.errors.field pattern)
   - Submit: form.post('/billing')
   - Cancel link back to /billing

3. Create `resources/js/Pages/Admin/Billing/Edit.vue`:
   - `defineOptions({ layout: AdminLayout })`
   - Props: billing (object), clients (array)
   - useForm initialized from billing prop. fecha_pago: billing.fecha_pago (may be null — that is correct, do NOT coerce to '')
   - Same form layout as Create.vue
   - Submit: form.put(`/billing/${billing.id}`)
   - Delete button with ref(null) sentinel modal pattern: confirm dialog, then useForm({}).delete(`/billing/${billing.id}`)
   - Cancel link back to /billing

4. Run `npm run build` to verify no compilation errors.
  </action>
  <verify>
    <automated>cd C:/laragon/www/hub-srojas && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fecha_pago: null" resources/js/Pages/Admin/Billing/Create.vue
    - grep -q "defineOptions.*layout.*AdminLayout" resources/js/Pages/Admin/Billing/Index.vue
    - grep -q "defineOptions.*layout.*AdminLayout" resources/js/Pages/Admin/Billing/Create.vue
    - grep -q "defineOptions.*layout.*AdminLayout" resources/js/Pages/Admin/Billing/Edit.vue
    - grep -q "router.get" resources/js/Pages/Admin/Billing/Index.vue
    - grep -q "preserveState" resources/js/Pages/Admin/Billing/Index.vue
    - grep -q "form.post.*billing" resources/js/Pages/Admin/Billing/Create.vue
    - grep -q "form.put\|form.delete" resources/js/Pages/Admin/Billing/Edit.vue
  </acceptance_criteria>
  <done>Three Vue pages render without build errors, Create uses fecha_pago: null, Index has filter dropdowns with preserveState, Edit has delete modal with sentinel pattern, npm run build succeeds</done>
</task>

</tasks>

<verification>
- `php artisan test --filter=BillingCrudTest` — 5 tests green
- `php artisan test --filter=BillingValidationTest` — 4 tests green
- `npm run build` — no errors
- `php artisan test` — full suite green (no regressions)
- POST /billing with estado=pagado and no fecha_pago returns validation error
- POST /billing with estado=pendiente and fecha_pago=null succeeds
</verification>

<success_criteria>
- BillingController has all 6 resource methods (no show)
- StoreBillingRequest and UpdateBillingRequest both have required_if:estado,pagado
- Routes registered under auth+admin middleware
- 9 tests green (5 CRUD + 4 validation)
- Create.vue initializes fecha_pago as null
- Index.vue has filter dropdowns with router.get preserveState
- Edit.vue has delete confirmation modal
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-facturacion/04-02-SUMMARY.md`
</output>
